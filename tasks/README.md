# tasks/

A dated, append-only log of every unit of work done on this project.

## Why this exists

Two reasons, and the second is the demanding one:

1. **Traceability.** Knowing *why* a decision was made six weeks later, without
   re-deriving it.
2. **Replayability.** The stated goal is that **someone could throw away the code and
   rebuild the whole solution from `tasks/` alone.** That is a high bar, and it has a
   concrete consequence: every task must record the **exact commands actually run**,
   not a prose description of them. If a command isn't written down, that step is not
   reproducible and the task is incomplete.

## Layout

```
tasks/
  README.md
  0001-<slug>/
    TASK.md          <- required
    <artifacts>      <- optional: logs, traces, generated files, screenshots
```

Numbering is sequential and never reused. Slugs are short and kebab-case
(`0003-first-gemm-trace`). A task folder is never edited to hide a failure — see below.

> **`0072` does not exist**, and it is the first gap in the sequence. The M13
> work was planned as six tasks; two of them merged during execution, and the
> benchmark sweep had already been created as `0073` and was being written to by
> a running measurement when the gap was noticed. Renaming it would have broken
> references from seven files and `tools/make_release.ps1`, and would have moved
> a directory out from under a live job. Recorded rather than repaired: a
> numbering gap costs nothing, and a broken link in a log whose purpose is
> traceability costs something.

## Failures are the valuable part

This is a learning project. A task that says *"tried X, it failed with error Y,
because Z, so we did W instead"* is worth more than one that says *"implemented X"*.

**Never delete or rewrite a failed attempt.** Append the correction. The dead ends are
the part that cannot be reconstructed from the source tree, and they are exactly what
saves time when the same wall is hit again.

## Template

```markdown
# NNNN — <Title>

- **Date** YYYY-MM-DD
- **Milestone** M<N> (from the project plan)
- **Status** in-progress | done | abandoned | superseded by NNNN

## Goal
What we are trying to achieve, in one or two sentences. If it is a gate, say what
passing looks like.

## Context
What was true before this task. Link prior tasks: `0002-slug/TASK.md`.

## What was done
Narrative, in order. Include the reasoning, not just the actions.

## Commands
```powershell
# Every command actually run, copy-pasteable, with the env assumed.
```

## Result
What happened. Include real output/numbers, not summaries of them.

## Problems hit
Each with: symptom -> cause -> fix. Include the ones we worked around rather
than solved, and say which is which.

## Artifacts
Files produced, where they live, whether they are checked in.

## Next
What this unblocks, and what the next task should be.
```

## Conventions

- **Numbers come from measurement, not memory.** Any performance figure in a TASK.md
  must be traceable to a trace file or a command output stored in the task folder.
  See `docs/05-measurement/` for what counts as a valid measurement — in particular,
  **wall-clock timing is never a valid NPU performance claim** on this project.
- **Link outward, don't duplicate.** Point at `docs/` for durable explanation and at
  `research/` for source material. `TASK.md` records *what happened on a particular
  day*; `docs/` records *what is true*.
- When a task invalidates an earlier conclusion, mark the old one
  `superseded by NNNN` and say why. Do not silently correct it.

## Index

| # | Task | Milestone | Status |
|---|---|---|---|
| [0001](0001-scaffold-and-research-index/TASK.md) | Scaffold repo, index research, write foundational docs | M0 | done |
| [0002](0002-m1-hello-npu/TASK.md) | Hello NPU — first design built, run, and traced (**gate passed**) | M1 | done |
| [0003](0003-m2-bf16-gemm/TASK.md) | bf16 GEMM on one core, traced — bfp16 flag worth **5.5×**; fp32 accumulate essential; DMA BD limit blocks `ffn_down` | M2 | done (single-core) |
| [0004](0004-m2-multicore-gemm/TASK.md) | Whole-array GEMM, traced — 99.8% core scaling but **compute is only ~7% of NPU time**; 8-col array is untraceable | M2 | done |
| [0005](0005-m3-python-reference/TASK.md) | Python reference encoder + goldens (**gate passed**, rel_fro ≤1e-6 vs HF) — and bfp16 is **6× worse on real activations** than on M2's uniform inputs | M3 | done |
| [0006](0006-m4-npue-pretiling/TASK.md) | `.npue` pre-tiled weight container (**gate passed**, round-trip bit-exact); M2's `tile_n=32` is **illegal at 8 columns**. *Its BD and performance claims are corrected by [0007](0007-m5-pretiled-gemm-on-npu/TASK.md)* | M4 | done |
| [0007](0007-m5-pretiled-gemm-on-npu/TASK.md) | Pre-tiled GEMM on hardware — `tile_n=48` **confirmed at 8 columns**, but pre-tiling is a **wash** and `ffn_down` never needed it on the whole array | M5 | done |
| [0008](0008-m5-bfp16-real-data/TASK.md) | bfp16 on **real activations, on hardware**: distribution barely matters (0.85×), refuting [0005](0005-m3-python-reference/TASK.md)'s 6.0×. *Its "two designs" trap is corrected by [0009](0009-m5-sync-misdiagnosis/TASK.md); the measurements stand* | M5 | done |
| [0009](0009-m5-sync-misdiagnosis/TASK.md) | The "NPU corruption" was **our own missing host→device sync** — `A.numpy()[:] =` never reaches the device, and only the first dispatch per process hides it. A falsifying sequence killed a theory that fitted every prior observation | M5 | done |
| [0010](0010-m5-b-reuse-and-cost-model/TASK.md) | B reuse **blocked** by two hardware limits, but a validated cost model — **t = 150 µs + traffic/33 GB/s**, ±1.4% — prices it at 1.26–1.68× and shows we are **dispatch-bound, not bandwidth-bound** | M5 | done |
| [0011](0011-m5-first-op-validated/TASK.md) | **GATE: first real encoder op on the NPU** — layer 0 QKV from `.npue` weights and golden activations, rel_fro **1.5e-03** vs the HuggingFace oracle. Confirms the `1/√32` fold landed in Q | M5 | done |
| [0012](0012-m5-all-layer-gemms/TASK.md) | **All four per-layer GEMMs validated** at both precisions. bfp16 damage is concentrated in `ffn_down` (1-cos 8.9e-03, 34-120x the others) because its input is post-GELU — heavy within-block dynamic-range tail | M5 | done |
| [0013](0013-m5-first-eltwise-kernel/TASK.md) | **First elementwise op on the array.** It runs — but IRON's built-in GELU lands at **1.33e-02**, and the three-way split blames the **LUT** (1.32e-02), not the tanh formula (2.0e-03). We write our own | M5 | done |
| [0014](0014-m5-own-gelu-kernel/TASK.md) | **Own kernel path works** (ExternalFunction + Peano), but fp32 intermediates bought only 1%. Error isolated to **`aie::tanh` itself (~1%)**. Trap: `aie::tanh<float>` silently emits an EMPTY function and the core hangs | M5 | done |
| [0015](0015-m5-gelu-polynomial/TASK.md) | **GELU without a transcendental**: `max(x,0)+poly(min(|x|,4))` exploits that `GELU(x)-max(x,0)` is even. **4.31e-03 on hardware, 3.1x better than `aie::tanh`**. *Its inference about AIE float precision is refuted by [0016](0016-m5-fp32-probe/TASK.md)* | M5 | done |
| [0016](0016-m5-fp32-probe/TASK.md) | **Refutes [0015](0015-m5-gelu-polynomial/TASK.md)**: AIE `vector<float>` carries a full **24-bit mantissa** on both add and multiply. fp32 LayerNorm and softmax stay achievable | M5 | done |
| [0017](0017-m6-full-encode/TASK.md) | **M6 GATE: full MiniLM encode on the NPU** — 24 GEMMs + GELU dispatched, **1-cos 2.05e-05** vs HuggingFace. M3's simulation predicted both the bf16 and bfp16 hardware results | M6 | done |
| [0018](0018-npu-vs-cpu/TASK.md) | **NPU vs CPU, first head-to-head.** GEMM engine **4.3-4.8x faster than CPU**, but a full encode is **21x slower** — 8.4 ms per dispatch of Python glue against 150 us of hardware | M8 | done |
| [0019](0019-offload-and-energy/TASK.md) | **Offload measured: the NPU path costs 159x less CPU** for 3.3x less wall time. CPU path occupies 11-17 threads; NPU path a third of one core. Package power not readable on this machine | M8 | done |
| [0020](0020-m5-layernorm-kernel/TASK.md) | **LayerNorm on the array** — first kernel with a reduction; passes at both sites (3.3e-03, 3.6e-03). Found: a core tile has only **2 in / 2 out DMA channels**, and the L1 budget caps this kernel at 20 rows/call | M5 | done |
| [0021](0021-m5-softmax-and-full-model/TASK.md) | **Softmax on the array, and a FULLY WORKING MODEL** — 24 GEMM + 6 GELU + 13 LayerNorm + 6 softmax all dispatched, **1-cos 3.4e-04** vs HuggingFace. Only per-head attention GEMMs remain on the host | M5/M6 | done |
| [0022](0022-m7-cpp-runtime/TASK.md) | **M7 GATE: a C++ runtime, no Python in the process** — mmap `.npue`, XRT dispatch, **rel_fro 1.507e-03, identical to the Python path**. Weights go to DMA untouched | M7 | done |
| [0023](0023-m7-full-cpp-encode/TASK.md) | **Full encode in C++, and a measurement that means something** — **42.4 seq/s at 0.20 cores**, 60x the Python path. Breakdown: 78% NPU path, 20% host attention. 1.50 ms per dispatch against 150 us of hardware | M7 | done |
| [0024](0024-m7-dispatch-cost-anatomy/TASK.md) | **What the 1.5 ms per dispatch actually is** — not host memcpy but **changing design**, at **~25 µs + 7.2 µs per lock** (89 µs to 2.4 ms). Not reconfiguration-by-difference, not residency, not eviction. Scales with descriptors, not columns — 4.1× spread at one column. Narrower therefore wins: 8 cols is 33% slower than 2. 42.4 → 52.4 seq/s | M7 | done |
| [0025](0025-m7-batching-and-crossover/TASK.md) | **Batching, and the width default as a prediction** — batch 16 gives **72.2 seq/s** (1.35x at constant width, 1.70x over 0023). Predicted 4-col crossover at M≈947, measured tie at M=1024. Two processes scale 1.46x *regardless of width* — host overlap, not array concurrency. Third fail-open cache bug, this one produced 1-cos 8.651e-02 | M7 | done |
| [0026](0026-m7-closing-on-cpu/TASK.md) | **Chasing CPU parity: 42.4 → 251.3 seq/s, and where it stops** — 6 routes measured. **Parity NOT reached** and shown unreachable: eltwise is 209 of 347 ms and a degree probe puts it at the machine's fp32 throughput limit (941 µs per Horner step, throughput not latency). Free eltwise would still only give 427 vs 710. **3.2× better per core.** Fourth fail-open, this one in the validation: NaN scored 0.000e+00 and PASSED | M7 | done |
| [0027](0027-m7-width-hypothesis/TASK.md) | **Is MiniLM the wrong shape for this machine?** — yes, measured. Elementwise work is **1 : 3h** of MACs, so its share falls as 1/h: **29.7% at h=384, 18.1% at h=768**, both within 7% of the structural prediction. Also corrects 0026 AND itself: the blocker was never arithmetic but per-core shim streams — **GELU now runs at 8 columns, 3.87x alone / 1.13x end-to-end**. Parity-h projection **withdrawn** (the CPU has the same 1:3h structure) | M7 | done |
| [0028](0028-research-index-nine-new-papers/TASK.md) | **Index nine new documents in `OthersResarch/`** — research only, no code. All 7 previously indexed PDFs still hash-match; 9 were new, each summarised to `research/papers/` as a genuine substitute, manifest and INDEX validated mechanically (16 papers, 0 problems). One PDF (`getting_peak`) is a textless *Print To PDF* of a blog post — identity confirmed by rendering four pages, recorded so nobody retries it. **Four corrections forced on our own documents**: trap 3's L1 budget is 63 KB not 64 (1 KB is program stack, two independent sources) and its inequality is the *Stationary C* form; `aie2p` has **5 accumulators, not the documented 8**, capping a bf16 kernel at 5 independent MMACs; **power mode belongs in every measurement** (turbo measured >22 pts worse than balanced); and `chess_*` pragmas are dead weight under Peano. Nothing in the nine contradicts a measured result of ours | M7/M8 prep | done |
| [0029](0029-m7-one-xclbin-probe/TASK.md) | **One xclbin, many instruction streams — step 0 confirms it** — two runtime sequences over one static design: xclbins differ only in UUIDs, a foreign insts stream reproduces all 6.3 M elements exactly, and alternation in ONE context costs alone-price (500 µs vs 497) while the two-context control pays 479 µs — 0024's model said 486. **The 49 switches per encode are removable without fusion** | M7 | done |
| [0030](0030-m7-expert-review-tests/TASK.md) | **Testing the expert review, end to end** — 7/10 claims confirmed on hardware, 1 refuted by an existing measurement, 2 deferred with a measured pricing. **251.3 → 298.5 seq/s** (eltwise at 8 cols) and **1-cos 3.397e-04 → 2.469e-04** (poly softmax, unblocked by the stack fix). Fifth fail-open closed with purge-before-build | M7 | done |
| [0031](0031-m7-eltwise-ilp/TASK.md) | **Row-interleaved LN and softmax, and the switch bill measured whole** — LN 1.54x, softmax 1.44x in isolation (the exp2 Horner had to be STEP-interleaved; sequential inlined calls gained zero), encode 296 → 305 seq/s at 1-cos 2.469e-04 unchanged. Fourth stack-trap bite — this one HANGS (timeout) instead of corrupting. And the headline: **~115 ms of the 420 ms encode is design switching (28%)**, measured design by design — a LayerNorm call is now 78% switch | M7 | done |
| [0032](0032-m7-one-xclbin-production/TASK.md) | **The one-xclbin encode: 305 → 611 seq/s, 1-cos 1.086e-05** — the spatial-split universal worker died on 16 KB program memory (softmax-il4 ELF alone is 12.2 KB); the pivot: no eltwise op earns a 2.3 ms switch, so LN, softmax and GELU moved to the host in fp32 — each move faster AND more accurate (M6's 1.09e-05 prediction landed exactly). The NPU is pure GEMM: four shapes as instruction streams over ONE xclbin (67-70 UUID bytes apart), one hw_context, **zero switches** — 24 dispatches at alone-price (3,142 µs). Wall vs CPU: 0.42× → **0.86×** | M7 | done |
| [0033](0033-m7-pipelined-lanes/TASK.md) | **Pipelined lanes: 611 → 833 seq/s — CPU WALL PARITY PASSED (1.17×)** — two encodes in two threads over the one unified design, every NPU interaction under one mutex (the array serializes anyway), each lane owning its A/C slots; lanes self-organize into anti-phase and overlap 1.49× — the two-process 1.46× reproduced in-process for free. Lanes verified BIT-IDENTICAL (memcmp over 3.1 M floats) at 1-cos 1.086e-05. Three lanes plateau: the C-readback is DRAM-bound (~340 MB/encode) — shrink the lane, don't add lanes | M7 | done |
| [0034](0034-m8-energy/TASK.md) | **Energy, measured on Windows after all — 1.94× better per sequence** — the blocker was the WRONG COUNTER SET: `\Power Meter` has no instances, `\Energy Meter` has 14 including RAPL_Package0_PKG. Units calibrated from the data (energy is picowatt-hours; two counter paths agree to 0.1%). Control experiment proves the meter sees the NPU (+9.1 W on a zero-host-work dispatch soak vs +3.9 W for its driver thread ⇒ NPU block ≈5.2 W). Differential method (two encode counts, subtract) cancels startup AND is immune to the idle drift. **44.0 J/1000 seq vs the CPU's 85.3**, three runs within 6% | M8 | done |
| [0035](0035-m8-mteb-gate/TASK.md) | **M8 GATE PASSED — MTEB cannot tell the NPU from the CPU** — five tasks (3 STS + classification + clustering), mean delta **+0.04 points**, worst single task −0.01, four of five within ±0.01. Built the bridge that made it runnable: `--encode-file` in the runtime plus an mteb 2.x encoder that tokenizes in Python and crosses to C++ as FILES (bridge self-test: 1.078e-05 vs sentence-transformers). Both sides pinned to seq 64 so the comparison is paired. **bf16+fp32-accumulate confirmed as production; bfp16 finally closed out** | M8 | done |
| [0036](0036-m8-tokenizer/TASK.md) | **The WordPiece tokenizer — TEXT IN, VECTOR OUT, no Python** — Unicode tables GENERATED from `unicodedata` (UCD 15.1.0) so they match by construction; **6,826/6,826 texts byte-identical to HuggingFace** at two lengths. The differential test caught two real bugs on its first run: I had implemented Python's final-sigma rule when the shipped model uses the FAST tokenizer (no context), and `[CLS]` in user text was being split. Vocabulary now rides inside the `.npue` (new U8 dtype) so deploying is ONE file. End to end 1-cos 6.10e-05, top-10 neighbour overlap 99.01%. Found a pre-existing bug: `pack_npue.py` has been broken since `gemm_b_layout` was factored out — the packer had not run in months | M8 | done |
| [0037](0037-m9-tiers-endpoint/TASK.md) | **Batch tiers + an OpenAI-shaped /v1/embeddings endpoint** — one xclbin now carries 16 streams (4 shapes x 4 batch tiers, all 15 identity checks 64-69 bytes), so requests are RIGHT-SIZED, not padded: a 1-text request went ~210 ms -> 15 ms, and 512 texts run at 918 seq/s. `--serve` verified against the official `openai` client (base64 included — it is the client's default). The endpoint test caught a real bug: extra lanes never inherited the tier table and silently kept the OLD flat slot contract, running the wrong GEMMs — sixth fail-open, now fails closed | M9 | done |
| [0044](0044-m9-optimisation-sweep/TASK.md) | **An optimisation sweep of IRON and four outside repos** — reading and pricing, nothing built. Twelve IRON features grep to **zero** in our tree (`pad_dimensions`, cross-tile `Buffer`, `CascadeFlow`, `consumer_obj_type`, `disable_synchronization`, hand-wired `TileDma`/`Bd`, …), all verified present in the installed wheel. `xrt::runlist` sits unused in our own XRT. The AIE default rounding mode is **`floor`** — a systematic bias in every bf16 store we have ever written. Two borrowed claims corrected before shipping: `pad_dimensions` is **mem-tile only** (so the output side needs a strided read), and the centred-polynomial-basis 2.5× was **refuted at fp32** — identical error to 4 s.f. until degree 10, because their fix targets bf16 coefficients and ours are fp32. And the one that changes a decision: **host eltwise costs 7.5% of an encode while the transport it forces costs 33%** — 0032 closed that question on the wrong ledger, and the expert review's §6b had already raised it, priced it ~2x low, and been left unblocked for two tasks. Also found and **closed the ninth fail-open**: a stale `npuembed.exe` held an Active hw_context and made `--bench` read 221.4 seq/s against a true 691.0, with nothing in the output to say so; `--bench` now refuses to run when the array is not ours (exit 2), failing closed on a missing `xrt-smi` too | M9 | done |

| [0038](0038-m9-model-driven-runtime/TASK.md) | **M9 Phase A: the runtime reads its geometry from the container instead of assuming MiniLM** — `--model`/`--list-models`, per-container guards, and a `pool_rows()`/`qk()`/`av()` refactor verified **bit-identical** (`--embed` sha256 unchanged) that turned out to hide a genuine **2× regression in host attention** (29→58 ms/encode) from replacing a compile-time 4-vector unroll with a runtime-bound loop — caught only by reading the `--bench` breakdown line by line, not the headline seq/s, and fixed with `qk_impl<NV>`/`av_impl<NV>` templated on `head_dim`. Also found `max_seq_len` (packed positions) was being read where the design's compiled seq length belonged; `backfill_design_seq.py` recovered it for 24/37 legacy design sets and refused 13 rather than guess. Net: **892.2 vs 844.8 seq/s (1.056×)** at batch 128 | M9 | done |
| [0039](0039-m9-bge-small/TASK.md) | **M9 Phase B: bge-small runs on the NPU first attempt** at `1-cos` **8.348e-06** (tighter than MiniLM's 1.086e-05) and passes the MTEB gate at mean **−0.03**, but costs almost exactly double the layers: **450.0 vs MiniLM's 888.7 seq/s (0.506×)**, 96 dispatches instead of 48. **Corrects the shipped docs' "byte-identical drop-in swap" claim** (`docs/00-overview.md`, `README.md`, `docs/04-model/README.md`, `CLAUDE.md`, all fixed in this task): it is 12 layers not 6, CLS pooling not mean — only tensor names and GEMM shapes carry over. Found four more places that mean-pooled unconditionally, one of which (`verify_npue.py`) reported `1-cos` 9.294e-02 and still printed PASS because the golden-ratio gate silently skipped models with no baseline. Disarmed a destructive bug twice: `make_goldens.py` wrote a hardcoded MiniLM filename regardless of `--model-dir`, which would have overwritten MiniLM's committed goldens with bge-small's | M9 | done |
| [0040](0040-m9-honest-cpu-baseline/TASK.md) | **M9 Phase C: `research/prior-art.md`'s prescribed ONNX Runtime CPU baseline is refuted — it is 0.48× of torch**, not the stronger opponent, so torch/sentence-transformers stays the comparator to beat. Interleaved, same statistic every side: **NPU/strongest-CPU 1.792× on MiniLM, 1.533× on bge-small**. Found the published CPU number was **best-of-5 against the NPU's mean** — re-measured symmetrically, best-of-9 gives 536.3 seq/s, well under the previously published 710, and the gap is **not fully explained** by the statistic (~3.4%) or background load (~6%); flagged honestly rather than reconciled. Caught its own fail-open: a hand-rolled battery check printed **"ON BATTERY"** for a machine with no battery, because `Win32_Battery` returns nothing and the `else` branch fired on absence — the ninth instance of this bug class. Made interleaving and steady-state mandatory in the measurement protocol | M9 | done |
| [0041](0041-m9-bo-allocation-flavour/TASK.md) | **Does FastFlowLM's `xrt::ext::bo` + 1 MB-padded allocation move DMA throughput over our exact-size `XRT_BO_FLAGS_HOST_ONLY`? No** — four modes isolating allocation class from size rounding all measure **device time identical to three digits (154.5–154.8 ms, a 0.2% band)** against 7% run-to-run noise, and the proposed IOMMU-page mechanism doesn't exist to be measured: sampled four times per mode, the mapped pointer's alignment is not a property of the mode at all (`ext` returns 64 KB half the time and 1–2 MB the other half). Keeps `XRT_BO_FLAGS_HOST_ONLY` as shipped. Caveat stated against its own conclusion: only tested at MiniLM's ~175 MB footprint; bge-large's ~604 MB is flagged as untested and carried forward | M9 | done |
| [0042](0042-m9-bge-large/TASK.md) | **M9 Phase D1: bge-large (h=1024, 24 layers, head_dim 64) runs first attempt at `1-cos` 8.432e-06**, and confirms [0027](0027-m7-width-hypothesis/TASK.md)'s synthetic-sweep prediction on a real model: **NPU/CPU ratio rises to 2.106× (4.2× per core)** as width grows, while depth pulls the other way (bge-small's same-width-double-depth measured 1.792→1.533×). `tile_n` stopped being a constant — N ∈ {1024,3072,4096} make 48 illegal, forcing **32** — and the packer's frozen `b_layout_hash` became a parameter, verified by reproducing the old hardcoded hash byte-for-byte. Found a fail-open: the packer's banner printed the intended `tile_n=32` while `prepare_model` still received a hardcoded `48`, caught only because the shapes happened to make 48 illegal — at a width where both were legal it would have shipped a mislabelled container. `--probe-bo` ruled out an XRT memory ceiling (1024 MB allocated in 0.10s; tested to 8 GB) | M9 | done |
| [0043](0043-m9-attention-geometry/TASK.md) | **M9 Phase D2: the plan's proposed unified attention+projection geometry `(m=16,k=64,n=8,cols=8)` does not compile** (`static_assert(n % (2*t) == 0)` fails), and the real microkernel constraint (`n ≥ 16`) combined with attention's `[64,64]×[64,64]` shape proves structurally that **any design expressing attention uses at most half the array (cols ≤ 4) and an 8× smaller output tile** than production's `(64,64,32)`@8-cols. Fixed a golden-lookup bug found en route: goldens were keyed on the **container filename**, so a second packing of the same checkpoint at a different `tile_n` couldn't find its own goldens; now matched on `source_sha256`. **Its own `## Results` section is left as the unfilled placeholder `(filled in below)`** — the four exported configurations (A production / B mid / C small-tile-full-width / D small-tile-half-width) isolate tile-size cost from column-count cost, but no throughput numbers were ever recorded here; only the structural, compile-time argument is backed by evidence in this file. The file also carries no `Date`/`Milestone`/`Status` header, unlike every other task | M9 | in-progress |
| [0045](0045-m9-bf16-gemm-epilogue/TASK.md) | **C leaves the array as bf16 — `--c-bf16`, +4.9% and the epilogue is free** — the first lever from 0044, built. fp32 accumulation over the whole K reduction is untouched (trap 2 intact); a core-local fp32 `Buffer` accumulates and a new `narrow_f32_bf16` kernel converts **once** into the bf16 fifo the DMA drains. **L1 unchanged** — the single-buffered accumulator costs exactly what the halved C fifo saves. **693.5 → 727.2 seq/s** (MiniLM), +5.0% bge-small, +3.1% bge-large, with per-dispatch `wait` moving ≤29 µs of 3,028: the epilogue disappears into the DMA shadow. `read out + bias` −20% at every geometry, **not** the −50% halving the bytes suggests, because only the *read* halved — C is still written as 679 MB of fp32 since everything downstream consumes fp32, which is why this is +4.9% and not the projected ~10%. Accuracy costs exactly one rounding (1.38–1.52× on `1-cos`, still **133–162× inside** the gate, neighbour overlap unmoved at 1.0000); **default stays fp32 until MTEB rules**, per 0035. Found: `accum::from_vector` narrows in the STORE while the project's documented multiply-by-1.0f idiom costs **34 emulated fp32 ops per 64 elements**; and `--c-bf16` collapsed the cache-marker namespace so `purge()` for `ffn_down` **deleted `ffn_up`'s build** — sixth cache fail-open, fixed by matching the ordered `aie.runtime_sequence` signature | M9 | done |
| [0046](0046-m9-b-reuse-asymmetric/TASK.md) | **B-reuse is not blocked by capacity or by the fifo API — there is no spare DMA channel** — the feasibility probe for `consumer_obj_type`, and it closes the lever with a census instead of a compile error. `b_reuse="asym"` fails on **"`repeat_count` unavailable for shim tiles"** (the replay must be driven by a mem tile), and `forward()`/`split()` do not expose `consumer_obj_type` at all. `"mega"` reproduces 0010's *"number of input DMA channel exceeded"* verbatim — but on **`row = 1`, a MEM TILE**, correcting 0010 and `gemm_pretiled.py`, which both blame a core tile and prescribe "a core-side redesign". The census (`tools/count_dma_channels.py`, new) on the SHIPPING design: **all 32 core tiles at 2/2 inputs, five of eight mem tiles at 6/6**. The mem-tile arithmetic is exact — A(1) + B(1) + C(4 core rows) = 6 — so **the C join spends the budget**, and freeing it needs `CascadeFlow` (C returned through one core, not four), which is also the one primitive that adds no descriptors. Baseline static counts validate the probe: 880 `aie.dma_bd`, 352 `aie.lock` → 2.56 ms implied switch cost against 0024's hardware-measured 2.4 ms | M9 | done |
| [0047](0047-m9-cascade-channel-probe/TASK.md) | **Cascade does not create DMA headroom — it trades three mem-tile inputs for three outputs** — answered by building upstream's cascade matmul rather than writing a kernel; it runs on our hardware with our dtypes (`kernels.cascade_mm` supports bf16→f32) and PASSes. At the SAME 4 columns: ours **6/6 in, 3/6 out**; cascade **3/6 in, 6/6 out**, with three of four cores per column having **zero DMA outputs** — they only cascade. 0046's prediction (`A+B+C(1)=3`) confirmed exactly. The trade is still the right one because **inputs** were the exhausted side. **And one fact worth more than the rest: every core tile is 2/2 inputs in BOTH designs** — a GEMM core needs A and B, a core has two channels, so nothing can ever hand a core a third stream, which is why 0020 had to pack γ+β into one buffer and why whisper-xdna packs K+V for fused attention. Caveats: upstream's cascade kernel is scalar-only (**3.19 GFLOPS** measured here), and `K % (4k) == 0` forces **k=32** at h=384 while bge-large works unchanged at k=64 | M9 | done |
| [0048](0048-m9-what-is-the-gemm-time/TASK.md) | **The GEMM is bound by the NUMBER of tile iterations, not by bytes — and that kills B-reuse** — `--bench` averages one `wait` over four shapes and cannot separate the accounts, so `--probe-streams` times each instruction stream alone. The production shapes contain a perfect control: **`ffn_up` and `ffn_down` have IDENTICAL MACs and differ 1.50× in bytes**, and measure **4,196 vs 4,273 µs** — 1.8% apart, in the wrong direction. `GMAC/ms` flat at 1.08–1.15; `GB/s` spreads 17.7–27.0. Reproduced on the `--c-bf16` set (27% more bytes on ffn_up, 0.3% LESS time). Fit `t = 573 µs + 4.72 µs × iterations`, ≤2.3% residual, against 0010's traffic model being 50% out on the discriminating pair. **Retires B-reuse (0010's 1.26–1.68× was priced with the refuted model) and the cascade milestone 0047 scoped**, since cascade only ever existed to free channels for it. Promotes tile SIZE, which is hard-blocked at 53,248 B of the 63 KB L1. And the successor question: a k-block costs **~5,900 cycles against 1,356 of arithmetic** — the array runs at **28–33 MACs/cycle/core against 145 traced in isolation**. Also flags a 10% disagreement between `--probe-streams` and `--bench` on absolute array time, left standing rather than smoothed | M9 | done |
| [0049](0049-m9-t16-iteration-anatomy/TASK.md) | **T16 answered — the "missing 4,500 cycles" never existed; the baseline was the bfp16-emulated datapath** — every row of 0007's "148.9–149.9 MACs/cycle traced" artifact carries `emulate_bfp16: true` and rel_fro 1.04e-02, and M2's plain-bf16 line always said 25.0; production (emulation off since M8) was being compared against the wrong datapath's trace. Reproduced today as a stable pair: emulation ON 1,340 cyc / 146.7 MACs/cyc / 1.04e-02, OFF 6,806 cyc / 28.9 / 1.89e-07. The traced anatomy of a production k-block: **7,813-cycle window of which 6,144 = exactly 768 MMAC steps × 8 `vmac.f`** — the fp32 datapath at its hard 32 MACs/cycle limit — plus 1,669 non-vector, 84-cycle gaps, zero lock/stream stall; the inner loop disassembles near-perfectly packed. At the documented 1.808 GHz the window+gap is 4.37 µs, **93% of 0048's fitted 4.72 µs** (0048's "5,900 cycles" used an implicit wrong clock). Under emulation the same design is DMA-bound (39% vector busy, lock-stall gaps) — M2's "starved, not slow" was an emulated-path finding, and it is why bytes measured free in 0048. **Re-prices T19 1.28× → ≈1.08× and T17 to ≤1.29×; the only multi-× array levers left are datapath changes** (bfp16 emulation = 2.9× of array GEMM time, an MTEB accuracy decision → T23; int8 → T20). Also: `run_one`'s printed avg mixes zero-kernel windows into the matmul mean, and the M=512 plain trace dropped packets — window histograms, not means | M9 | done |
| [0050](0050-research-atb-paper/TASK.md) | **The ATB paper (2511.16041), dug out of a user-linked French decode repo** — the repo itself (FastFlowLM/Qwen GEMV throttling) offers nothing: it is the bandwidth-bound regime we are not in. Its citation is the keeper: UCLA+AMD measure **24.3 TFLOPS BFP16 GEMM on our SKU with our toolchain** by decoupling A's buffered M from C's (ρ ≥ 1) — tiles that need 111 KB symmetric fit in 57.4 KB — plus **0.32 → 0.92 TFLOPS/core (2.88×) from microkernel hand-optimisation alone**. All of it on the MMAC datapath, so it re-prices T23's ceiling (~8× array time, floor 2.9×) and hands T19 a safer mechanism (shrink A, keep everything double-buffered), while changing nothing on today's plain-bf16 path. First web-only entry in the paper index | M9 | done |
| [0051](0051-m9-bge-base-and-in-exe-fetch/TASK.md) | **bge-base-en-v1.5 — the model this NPU wanted — and the download moved into the exe** — four measured requirements (every N a multiple of 384 so tile_n stays 48; head_dim 64; wide not deep; WordPiece/post-LN/absolute) and bge-base is the only common embedder meeting all four. It packs to the **same layout_hash as MiniLM and bge-small**, builds ONE xclbin with 16 streams at 8 columns first attempt, and validates at **1-cos 1.353e-05** on hardware / **2.613e-05 end to end** with top-10 overlap 1.0000. **181.2 seq/s against my own ~230 prediction — recorded as a miss**, because 0048's iteration fit was extrapolated across a width doubling it was never tested at; the signal it did give is that **the NPU is 74.1%% of wall here against MiniLM's ~40%%**, the most array-bound model we have run. Part 2: `get-model.cmd` ran `curl` then compared a `certutil` digest to a hardcoded constant — the literal signature of a dropper, so AV flagged it. Now `npuembeddings list` / `serve <model>` / `embed <model>` fetch over **WinHTTP inside the exe**, verify the pin with the packer's own `sha256_file` (not a fifth copy), and **cross-check the catalogue geometry against the downloaded config** before packing. Containers come out **byte-identical** to the repo's; wrong weights and wrong config both refuse. Found a bug in my own first cut: `default_root` required `models/`, which does not survive a zip | M9 | done |
| [0052](0052-m10-research-night/TASK.md) | **The 0.3.0 research night — bfp16+bf16C passes every gate, and two threads die on hardware** — T24 closed with one command (the 0048 fit holds at h=768; 0051's 27% miss was the un-modelled host term, and the array account closes 1,047 vs 1,053 ms). Lane sweep: **default 2 was leaving 19% on the table** — bge-base 175→209 seq/s, MiniLM 893→963 at 4 lanes; the subcommand default is now 4. The headline: **bfp16 emulation + bf16-C measures 1-cos 3.615e-04, e2e 9.7e-04, neighbour overlap 0.9923, MTEB mean +0.16 / worst −0.06 — every accuracy gate passes** at 2.20× array GEMM and 1,046 seq/s (+9% e2e, host-walled at 47% NPU busy). The combination is **6.6× more accurate than bfp16+fp32C, which no known mechanism explains** — filed as T26 with a named, testable hypothesis (the fp32-C fifo path re-quantising partials per k-block). The emulated array is TRAFFIC-bound (ffn_up slower than ffn_down by its byte ratio), so 0048's retired byte-levers revive on that path (T27). And **T19 closed negative on hardware**: k=96 Stationary-B is 5.2% worse per MAC — compute scales perfectly but the exposed B fill (gap 84→1,193 cyc) costs double the amortisation gain. Contaminated first traces (MTEB holding the array, dropped E1 packets) were discarded and re-measured idle, not smoothed over | M10 | done |
| [0053](0053-m10-t26-probe-bge-base-mteb/TASK.md) | **T26's hypothesis dies on the object code; bge-base clears the MTEB bar too** — a controlled full-K-vs-split-K, fp32-C-vs-bf16-C matrix **refutes** "the fp32-C fifo re-quantises at k-block boundaries": the matmul kernel is byte-for-byte identical between builds, and the full/split error ratio is flat 1.000× at both K=384 and K=1536 — it does not grow with block count. The 6.6× production anomaly does not even reproduce on an isolated synthetic-data GEMM (bf16-C ties or loses to fp32-C at full K); a real but smaller 27–36% bf16-C advantage shows up only in single-k-block dispatches, pointing at a **new hypothesis: rounding-mode asymmetry** (`conv_even` on-core vs the AIE default `floor` used elsewhere) compounding across the 24-GEMM chain — untested. **bge-base-en-v1.5 MTEB: both plain-bf16 (mean +0.02, worst −0.003) and bfp16+bf16C (mean −0.065, worst −0.193) PASS** on an independent geometry, and the interleaved ratio is **1.633× over torch** (mains power, 0040 protocol). Filed the missing **T28** phase-fusion thread (a three-task registry gap). Two harness bugs found and worked around, not fixed: `run_mteb.py --sides npu` cannot self-compute a gate (merged manually against the recorded CPU column instead of re-running it), and `compare_three.py` crashes in its own closing `print` — after the artifact is already written correctly | M10 | done |
| [0054](0054-m10-phase-fusion-pipeline/TASK.md) | **Phase fusion, Del B — the epilogue survey extended to all three production shapes, and the first working cross-core GEMM→mem-tile→GELU pipeline** — B1: `epilogue="gelu"` (0030's single-shape proof) now measured at MiniLM/bge-small (rel_fro 3.167e-04, reproducing 0030 exactly as this session's control), bge-base (2.102e-04) and bge-large (1.764e-04, needed a new `gelu_epilogue_2048_f32` entry point for its tile_n=32); traced per-core overhead is only **~5%**, far below the **1.6–2.7×** wall-clock dispatch-latency gap against a bare GEMM — flagged, not explained. B2: a genuine cross-core pipeline — GEMM core output flows through a mem tile straight into a DIFFERENT core's GELU, one dispatch, **no C tensor in the design's I/O signature at all** (`rt.sequence(A, B, Y)`), correct to rel_fro 9.052e-04 (a `--identity` routing-only variant measures 3.550e-08). Getting there took **five wrong designs, each a genuine new IRON trap**: `ObjectFifoLink` forbids being both a join-destination and a split/forward-source (confirmed in the library source — N:M through one buffer is not just misconfigured, it is inexpressible); a bare point-to-point `ObjectFifo` compiles but **hangs the hardware** with zero diagnostic; a **hand-built `TensorAccessPattern` for a plain full-region copy also compiles clean and hangs** — found via a from-scratch diagnostic that reproduced the hang with NO cross-core routing at all, fixed by swapping in `TensorTiler2D.simple_tiler`; a mem-tile join needs `gemm_pretiled.py`'s own `dims_to_stream` unscrambling formula even on a 2-tile case (wrong-but-finite if omitted, not a crash); and that fix appeared to do nothing until a full cache purge, because the JIT's marker was too coarse to notice a `dims_to_stream`-only change — a third instance of the marker-specificity fail-open class. The full production ffn_up→GELU→ffn_down three-stage chain was **not attempted**: ffn_down's K-reduction needs ffn_up's entire N=4h output, split across every GEMM column, which is a many-to-many regather the N:M restriction may make inexpressible — an open question for a future session, not a build to retry harder | M10 | done |
| [0055](0055-m10-embeddinggemma-spike/TASK.md) | **EmbeddingGemma-300M spike (Del C, C1 only) — numpy oracle matches HuggingFace to 1-cos 2.110e-08, geometry says `tile_n=16`@8cols, and the honest prior is ~62-72 seq/s against bge-base's measured 181-209** | M10 | done |
| [0056](0056-m10-t26-rounding-and-chain-probe/TASK.md) | **T26's rounding-mode hypothesis dies too, but a chained probe finds the real mechanism class** — `runtime/src/main.cpp`'s `to_bf16` (mirrored in `tools/npue.py`'s `to_bf16_bits`) is round-to-nearest-even, not `floor`, at the ONE downstream narrowing site both the fp32-C and bf16-C paths share — no floor-vs-`conv_even` asymmetry exists anywhere in production. But a new chained-GEMM probe (`t26_chain_probe.py`, 2 device builds reused across every stage) shows the refined structural hypothesis — bf16-C narrows the raw accumulator once EARLY, before bias, in addition to the shared late RNE narrow — **genuinely compounds**: tied at 1 stage (`1-cos` ratio 0.99, reproducing 0053's isolated-GEMM tie), diverging monotonically to **1.67× by 4 stages**, the right order of magnitude to plausibly explain production's 6.6× over a 24-GEMM encode. The numerical *why* (independent per-block quantisation noise averaging vs single-shot narrowing?) stays open | M10 | done |
| [0057](0057-m10-t28-cross-column-regather/TASK.md) | **T28's cross-column regather — answered: expressible at reduced scale, precisely port-blocked at production scale** — a join's own `.cons()` feeds a THIRD on-chip Worker with no second link (rel_fro 3.550e-08, the restriction 0054 found is about calling split/join/forward TWICE on one object, not about reading a join's consumer side); cross-column JOIN routing works at ANY distance, adjacent or full-array-span, identically (rel_fro 3.474e-08 both). The wall is the mem-tile 6-in/6-out port budget, now stated BY THE COMPILER at the exact point it bites (`"requires 8 input... only 4 available"`, `"...7 input... only 6 available"`, `"...only 0 input/5 output available"` for three different configurations) — an 8-way single join cannot express the full ffn_down regather, but the arithmetic for a hierarchical 2-hop merge (two 4-way joins, each exactly at budget, read by a relay core's 2 input channels) closes; not built | M10 | done |
| [0058](0058-m11-iron-1.4-migration/TASK.md) | **mlir-aie upgraded 1.3.4 → 1.4.x (main): 15 IRON design scripts broken by the `Runtime` API rewrite, 14 re-verified bit/value-exact against historical numbers** — SAXPY 335/541,662 cyc, GEMM 25.0→137.2 MACs/cyc (bf16→bfp16) and 141.7 MACs/cyc whole-array, all four eltwise kernels' floor/RNE error pairs (GELU 4.312e-03→2.494e-03, LayerNorm 3.326e-03→2.059e-03, softmax 4.278e-03→3.325e-03), `gemm_pretiled.py`'s 140.9 MACs/cyc rowmajor ffn_down, four M10 pipeline/join probes (3.550e-08 / 3.474e-08 family), and `build_passthrough.py`'s exact "67 bytes differ, all identity metadata" xclbin invariant — all exact or noise-floor matches to the numbers already on record. **1 of 15** (`unified_design.py`) migrates cleanly but cannot run to completion on ANY toolchain version — the pre-existing task-0032 16 KB program-memory wall, not a regression. Found and left open: `tools/export_gemm_rtp.py` (not one of the 15, but downstream of `gemm_pretiled.py`) is broken by an unrelated MLIR pretty-printer format change (`<size=,stride=>` → `sizes=[...] strides=[...]`) that defeats its cache marker — blocks rebuilding shipped production xclbins on the new toolchain, fix not attempted (out of scope). Also found and worked around: a fourth instance of the marker-specificity stale-cache fail-open class, this time inside `@iron.jit`'s own lookup layer rather than any script's `purge()` | M11 | done |
| [0059](0059-m11-production-verify-post-migration/TASK.md) | **Production confirmed bit-identical after the mlir-aie 1.4 upgrade — not just assumed** — full clean C++ rebuild succeeds with zero Python/IRON involved; all four shipped models reproduce their exact pre-upgrade `1-cos` on the validation encode (MiniLM 1.086e-05, bge-small 8.348e-06, bge-base 1.353e-05, bge-large 8.432e-06 — identical to the recorded digit, not just "under the 2e-03 gate"). `verify_embed_e2e.py` matches MiniLM to 4 s.f. against its saved baseline and reproduces bge-base's output JSON byte-for-byte; bge-small/bge-large get a first-recorded e2e baseline (no prior one existed at these artifact sets). Throughput sanity check (887.0 seq/s MiniLM, explicitly not a performance claim) consistent with history | M11 | done |
| [0060](0060-m11-export-gemm-rtp-marker-fix/TASK.md) | **`export_gemm_rtp.py`'s marker fixed and proven correct, not just non-crashing** — the new `aie.dma_bd` textual form (`sizes=[...] strides=[...]`) confirmed directly by grepping fresh `aie.mlir` across all four production shapes: B's dma_bd always ends `sizes` in `[.., k, n]` right before `strides`, exactly twice per build, nowhere else. `markers_for()`'s marker becomes `f"{k}, {n}] strides = ["`. Verified on a fully purged cache at both a small case and the real production shape (`--batch 128 --cols 8`), and — beyond "it doesn't crash" — a fresh export combined with the shipped weights reproduces `0038`'s/`0059`'s exact historical MiniLM `1-cos` figures on hardware (1.086e-05 golden, 2.644e-05 end to end), in a separate directory that never touched `runtime/artifacts_b128il/`. Closes T22 | M11 | done |
| [0061](0061-m12-embeddinggemma-tokenizer/TASK.md) | **EmbeddingGemma-300M's C2 tokenizer — and the plan's own architecture assumption was wrong** — reading `tokenizer.json` directly showed `model.type == "BPE"` with 514,906 merges, not the Unigram/Viterbi model the plan assumed; implementing Unigram against this checkpoint would have compiled and silently mis-tokenized everything. Built instead: a build-time Python table generator (`tools/gen_gemma_tokenizer_table.py`, 262,144-vocab + task-prefix table → an 8.60 MB flat binary) and a standalone C++ tokenizer (`runtime/src/tokenizer_gemma.cpp`, metaspace normalisation, linked-list+lazy-heap BPE merge, `<0xNN>` byte-fallback, `<bos>`+task-prefix+text+`<eos>` wrapping) — zero XRT dependency, not wired into `main.cpp`/`hub.cpp`. Verified against live HuggingFace tokenization: **1,925/1,925 byte-for-byte identical** (210 adversarial sentences × 5 task-prefixes, plus 1,715 byte-fallback stress codepoints × 5 prefixes), including truncation, padding and the no-prefix control | M12 | done |
| [0062](0062-m11-t28-hierarchical-merge/TASK.md) | **T28's hierarchical 2-hop merge, built with REAL compute at every stage, not the diagnostic identity-copy 0054/0057 used** — 4 GEMM+GELU producer columns (real `kernels.mm()` GEMM → GELU epilogue narrowing straight to bf16) → two merge mem tiles each `.join()`-ing 2 producers while also carrying their own local A/B feed (the "mem tile does double duty" topology 0057 priced) → one relay core reading both merge tiles directly via `.cons()` (0057's Q1 mechanism, no `.forward()`), hop-by-hop acquire/release, feeding a real second-stage matmul producing a genuine ffn_down-shaped output. Checked against an independent fp64 reference: **rel_fro 1.726e-03**, reproduced at a different seed and bit-identically on repeat. New IRON trap found and fixed: pulling more than one `ExternalFunction` symbol from the same multi-symbol `.cc` file duplicate-symbol-fails the link — every requested symbol compiles the WHOLE file, no per-symbol extraction; fixed by splitting the three relay kernels into separate files. Scale reduced from 0057's GROUP=4 boundary to GROUP=2/bf16-gathered/N_DOWN=16, since a real second-stage matmul needs L1 for a resident weight+accumulator on top of the gathered hops, and GROUP=4's one-shot gather alone already exhausts that with nothing but a copy kernel in it — documented, not solved. T28 left OPEN | M11 | done |
| [0063](0063-m12-embeddinggemma-kernels/TASK.md) | **EmbeddingGemma-300M's C3 host kernels — RMSNorm, RoPE, GeGLU, verified against real tapped intermediates from tasks/0055's checkpoint run, 36/36 records PASS** — one `rms_norm_cpu` (double-precision reduction, the `(1+w)` form) serves both the full-hidden and per-head `q_norm`/`k_norm` uses; per-head RMSNorm and all 4 GeGLU cases are **bit-exact (rel_fro 0.0)**, full-hidden RMSNorm 0.0–2.75e-11, RoPE 1.7e-8–4.3e-8 (float32 ULP floor, confirmed by comparing `std::cos`/`std::sin` against numpy's). GeGLU needed the reference's exact two-stage float32 rounding (round `act`, THEN promote and multiply by `up`) to go from ~1e-8 to bit-exact — a single fused double-precision expression, though mathematically equivalent, was not. Test harness gates on `rel_fro`, not a fixed `max_abs`, after a false FAIL on `L23.ln_post_ffn` (activations reach \|x\|≈28,357 there — `max_abs` 9.8e-4 was one float32 ULP, not a bug). Negative control, run against the same real tensors: dropping RMSNorm's `1+`, swapping RoPE's per-layer theta, and using erf-GELU instead of tanh-GELU measure rel_fro 0.439 / 0.757 / 3.26e-4 — 4-6 orders of magnitude worse than the real result. Standalone only, same discipline as 0061: no wiring into `main.cpp`/`hub.cpp`/the packer | M12 | done |
| [0064](0064-m12-embeddinggemma-arch1-integration/TASK.md) | **EmbeddingGemma-300M's arch=1 goes end to end in production C++, host-only, verified to `1-cos` ~1e-12** — corrected the task brief's premise first (the checkpoint's 314 safetensors tensors are ALL F32, not bf16, checked from the raw header, not assumed); `tools/pack_npue.py` gained `pack_gemma()`, storing every GEMM operand PLAIN (F32, row-major, no NPU tiling — there is no NPU kernel for this arch, so the whole tile_n/L1-budget/DMA-BD machinery, and the MQA tile_n=16 geometry tax tasks/0055 priced, simply does not apply), 317/317 tensors round-trip bit-exact against the source checkpoint. New `GemmaEncoder` (`runtime/include/gemma_encode.hpp` + `.cpp`) runs the full 24-layer forward pass on the CPU (double-accumulated GEMMs, MQA collapsed to direct K/V reuse since `num_key_value_heads=1`, tasks/0061's tokenizer + 0063's RMSNorm/RoPE/GeGLU kernels) and is wired into `main.cpp`'s production `Encoder::run()` as a genuinely separate early-dispatch path — not a branch inside it, since `Encoder` requires seven NPU `Design&` this arch has none of. Checked against `reference/encoder_gemma.py` on two independent 4-sentence corpora tokenized with the real HuggingFace tokenizer: worst `1-cos` **4.969e-12** and **5.496e-13** — tighter than any other model's gate in this project, expected since nothing here rounds through bf16. `npuembed.exe --model embeddinggemma-300m --embed` reproduces the standalone CLI bit-for-bit; the BERT path's MiniLM golden check still reports its exact historical `1-cos` 1.086e-05 after rebuild, unaffected. `hub.cpp` gained a `gated` `CatalogEntry` field and fail-closed `HF_TOKEN` bearer-auth support, but deliberately NO catalogue row for EmbeddingGemma itself — no session has ever held `HF_TOKEN` to pin a verified sha256, and `ensure_model()`'s packing step is still BERT-only C++. `--serve` on this arch refuses loudly (no HTTP endpoint built) rather than silently doing nothing. Explicitly not done: the C++ packer mirror (`npue_pack.cpp`), any speed work (batching/AVX2/threading — ~7.9 s/sentence unoptimised), and MTEB | M12 | done |
| [0065](0065-m12-embeddinggemma-cpp-packer/TASK.md) | **The C++ packer mirror for arch=1 — the largest gap tasks/0064 left open, now closed byte-for-byte** — `runtime/src/npue_pack.cpp` gained `prepare_model_gemma()`, the direct mirror of `pack_npue.py`'s `pack_gemma()`: `Writer::write()` grew one new parameter (`arch`, default 0, so the BERT call site is unchanged); a new `add_gemm_b_host()` sits next to the existing tiled `add_gemm_b()` and is *less* code (transpose + raw F32 copy, no tiling, no `layout_hash` — there is no NPU kernel for this arch); `main.cpp`'s `--prepare-model` CLI gained an early `config.json["model_type"]` dispatch mirroring the Python `main()`'s own. The one real risk — reformatting `rms_norm_eps`/`rope_theta`/`rope_local_base_freq` and disagreeing with Python's float-to-string in the last digit — was sidestepped by copying the config.json literal substring verbatim rather than reparsing it, after confirming by direct `json.dumps(json.loads(x))` round-trip that this checkpoint's exact literals (`1e-06`, `1000000.0`, `10000.0`) are already canonical. **Result: `tools/verify_pack_parity.py` (extended to detect arch by `model_type` rather than assume `vocab.txt` exists) reports byte-identical `sha256` for both packers' 1,239.65 MB / 317-tensor output**, confirmed independently by a second `sha256sum`; the BERT path stays byte-identical too (regression check, unchanged 69.02 MB). Re-ran tasks/0064's own correctness gate against the FRESH C++-packed container rather than assuming parity implies it: worst `1-cos` **5.496e-13** / **4.969e-12** on the same two corpora, identical to the recorded digit, and the raw encode output `cmp`-matches tasks/0064's own bit-for-bit | M12 | done |
| [0066](0066-m12-embeddinggemma-hf-token-fetch/TASK.md) | **The real gated fetch, tested with a real `HF_TOKEN` — and two integration bugs found by testing a genuinely fresh root** — the official `google/embeddinggemma-300m` download is **byte-identical** (sha256 `cbf5a78393b6a033...`) to the `unsloth` mirror every prior task verified against, so `hub.cpp`'s `table()` now carries a real, verified catalogue row instead of an empty pin. Fresh-root testing (not the already-populated checkpoint dir) surfaced what a warmed-up test never would: the `embed <model>` subcommand's post-fetch dispatch was BERT-only and threw "no NPU design for hidden 768" before Gemma's own arch dispatch ever ran — fixed with an arch-aware short-circuit right after `ensure_model()`, deliberately keeping `run_gemma_mode()`'s own call OUTSIDE the narrow try/catch that only guards the arch peek, so a real Gemma error propagates instead of being swallowed into the wrong failure path. Deeper, **not fixed**: `gemma_tokenizer.bin` has no C++ generator — it is Python-only (`tools/gen_gemma_tokenizer_table.py`, CLAUDE.md rule 5: no Python at runtime) and is not part of the HuggingFace checkpoint, so a from-scratch token-only fetch cannot self-produce it; worked around for this test by copying the already-generated table, and the full path then reproduced the already-verified reference container's output **bit-identical** on the same input. The token itself was handled only as a same-invocation `$env:HF_TOKEN`, never written to any file | M12 | done |
| [0067](0067-m12-gemma-tokenizer-cpp-and-token-flag/TASK.md) | **The last EmbeddingGemma fetch gap closed: a C++ tokenizer-table generator, plus `--token`** — a from-scratch, dependency-free JSON DOM parser (`json_min.hpp/cpp`, recursive descent, unordered_map objects for the 262,144-entry vocab, order-preserving vectors for the 514,906 merges) backs a line-by-line C++ port of `tools/gen_gemma_tokenizer_table.py` (`gemma_tokenizer_gen.hpp/cpp`), every validation guard preserved. `prepare_model_gemma()` now generates-and-caches `gemma_tokenizer.bin` instead of only reading one. **Verified against real ground truth, not self-reference**: the C++ output matches the genuinely Python-generated reference at **sha256 `c7a03c2c35ffc2a16b5513bb11c3d04e4a19c84acb9254b36765510acbf5bc81`, both exactly 9,020,206 bytes, `fc /b` "no differences encountered"** — first from a standalone throwaway verifier, then again from the real `npuembed.exe --prepare-model` with the cached file deliberately removed, confirming the production wiring, not just the generator function in isolation. Parse+generate measured at 2,793 ms wall clock (host tool, not an NPU claim). Second, independent deliverable: `--token <value>` on `embed`/`serve`, taking precedence over `HF_TOKEN` when both are set, failing closed with a dual-option message before any network call when neither is — all four precedence cases confirmed with a temporary instrumented throw (reverted before the final build) rather than a real network request, since no real token was available or used | M12 | done |
| [0068](0068-m13-nomic-spike-and-oracle/TASK.md) | **nomic-embed-text-v1.5 spiked as the fifth model — and the probe found a silent correctness bug every shipped model sits one step from** — `reference/fetch_model.py` was deliberately run against nomic BEFORE being taught about it, so its BERT-only check downloaded first and then enumerated every departure mechanically (112 tensors present / 197 required); that failure list answered three of seven open architecture questions with no experiment at all: **no biases on any of the five projections**, **no `position_embeddings` tensor whatsoever**, and `emb_ln` at top level rather than `embeddings.LayerNorm`. The file is now arch-aware, refuses an *unknown* `model_type` rather than checking it against BERT's inventory (112/112 OK, all four shipping models unaffected), and demands the *absence* of a position table — a nomic checkpoint that shipped one would mean the rotary branch is not live. The remaining three questions were settled empirically with discriminating negative controls separating by ~7 orders of magnitude: **post-LN** (2.401e-07 vs pre-LN 5.031e+00), **`out = fc11(x) * silu(fc12(x))`** — SiLU on **fc12** (1.636e-07 vs swapped 4.022e+00), and `mlp.norm` is `Identity()`; cross-checked bit-identically against HuggingFace's native port. `vocab.txt` is **byte-identical across all five models**, so the C++ WordPiece tokenizer serves nomic unchanged by transitivity. `config_sentence_transformers.json` carries **no `prompts` dict**, so the task-prefix table is this project's own choice and must be labelled as such. **The headline finding is not about nomic:** asking "does N=6144 compile?" produced "yes, and it returns wrong numbers" — tasks/0030's C-drain stride guard forces `tb_n_rows = 1` above `m·n_aie_rows·N > 2^20` and `C_tiles` honours it, but the fill loop computed its own count from a hardcoded `tb_max_n_rows // 2` and never read it, so drains covered 1 row-block while fills streamed 2 (`rel_fro` 7.07e-01, 28/32 row-bands wrong). **bge-large's production N=4096 is EXACTLY 2^20 and the test is a strict `>`, so the guard had never fired in a shipped design** — the file's own comment records the near-miss ("measured: N=4096 at exactly 2^20 builds") without drawing the conclusion, because measuring *at* a boundary never exercises what lies beyond it. Fixed by driving the whole row-block walk from the guarded value, which at the historical `tb_n_rows = 2` reduces algebraically to the original — provably a no-op below the threshold. Gates: N=6144 7.076e-01 → **3.283e-07**, the three below-threshold shapes reproduce, the M=256 single-row-block tail still correct, and fixed-vs-unfixed xclbins differ by **74 bytes**. Corrects `docs/CURRENT_STATUS.md`, which listed `hidden ≥ 1536` as an open *build* wall — it builds and returns wrong numbers, which is strictly worse. Newly recorded: the shipped `artifacts_base` xclbin **cannot be reproduced byte-for-byte from today's toolchain** (3,457 bytes, from the mlir-aie 1.3.4→1.4.x upgrade, reproduced with the unmodified generator). → **T30** (answered), **T31** (filed) | M13 | done |
| [0069](0069-m13-nomic-arch2-container/TASK.md) | **`arch=2` — container, oracle, design, and a fail-open caught before commit** — `pack_nomic()` emits the *same tensor names and emission order* as arch=0, so `Encoder::stage_all()` and the whole NPU dispatch path work unchanged; `fc11` and `fc12` fuse into ONE `[768, 6144]` `ffn_up` so the array still sees four GEMMs per layer, with the halves recorded in the file as `swiglu_halves="fc11_up|fc12_gate"` rather than left as a convention to remember. Every config fact it depends on is **asserted** (`rotary_emb_base == 1000`, `prenorm == False`, the three bias flags, NeoX RoPE, activation), so a checkpoint that changed underneath refuses to pack instead of packing wrong. 322.08 MB, 150 tensors, `layout_hash 94266693ea31aa67…` — the constant MiniLM/bge-small/bge-base share. Round-trip **113,246,208 bf16 elements, 0 differing**, the fused `ffn_up` included. Bias and position tensors are zero-filled (nomic has neither) and `verify_npue_nomic.py` asserts they are exactly zero **and** that the same check finds them non-zero in an arch=0 container, so the test can fail; the Q scale fold is **proved** exact through RoPE (`rel_fro` 0.000e+00 — exactly zero, because 1/√64 = 0.125 is a power of two, which does *not* generalise to MiniLM's 1/√32). Oracle `reference/encoder_nomic.py` gated against TWO independent implementations (transformers' native `nomic_bert` port and the remote code via `SentenceTransformer`) that agree to 1.356e-06 *before* either is trusted: final embedding **`1-cos` 3.573e-13**, six orders under the bar and tighter than this project's own BERT oracle, with controls at 1.876e-02 (wrong theta) and 8.879e-01 (wrong SwiGLU half). Discovered en route: the original remote code cannot expose per-layer hidden states at all, which forced the native port to be the boundary-tap source. `export_gemm_rtp.py` gained `--intermediate`/`--gated-ffn` (its hardcoded `4*hidden` was an assumption wearing a constant's clothes); the N=6144 design built first attempt — **ONE xclbin, 16 streams, all 15 identity checks 64–75 bytes** — and is the first shape here to execute 0068's guarded path. **T31 fixed**: `design_fits()` matches the streams' `(op, K, N)` against the container's geometry, reusing `parse_streams()` so it closes the hole on design sets exported before the geometry keys existed, no re-export; falsification test — with `artifacts_nomic` moved aside and `artifacts_base` still present, nomic reports `no design` while bge-base stays `ready`. Also fixed an unfiled fail-open (`print_catalog` reported Gemma `ready` whenever any hidden-768 design was present, despite arch=1 having no NPU kernel; now `cpu`). **And one this commit nearly shipped:** with container and design in place `list` said `ready` for nomic — true about designs, wrong about outcomes, since `Encoder::run()` is a BERT forward pass and arch=2 reuses BERT's names *deliberately*. `set_model_shape()` now refuses (exit 2) as a **whitelist** of implemented architectures, with `encoder_implemented()` the single source both the table and the refusal read. Regression: 4.473e-03 / 3.789e-03 / **4.297e-03** / 3.763e-03, bge-base identical to the figure in 0051 | M13 | done |
| [0070](0070-m13-nomic-runtime/TASK.md) | **nomic runs on the array — RoPE + SwiGLU inside the shared `Encoder`, and a data race the golden gate could not see** — `apply_rope_qkv()` rotates Q and K **in place inside the fused `[B][S][3·hidden]` buffer** (never V), avoiding the `[B,H,S,D]` repack the Gemma path does; tables built once per encoder from the existing `npue::gemma_rope_tables()`, which is model-agnostic NeoX construction despite the historical name. `swiglu_cpu()` does `fc11 * silu(fc12)` over the fused `[hidden, 2·inter]` `ffn_up`. Both sit behind `g_rope`/`g_gated_ffn`, and the arch=0 FFN code is unchanged — just moved inside an `else`. Hardware `1-cos` **2.599e-05**, e2e worst **2.401e-05** with top-10 overlap 1.0000, and the four shipping models return **identical** figures with `--embed` output bit-identical by sha256. **The orchestrator's own algebra was wrong, and hardware found it.** The spec for this task asserted `swiglu_cpu` could compact forward in place and supplied a proof; the proof showed no row `r' > r` could have its read range clobbered — and never checked `r' < r`. Row `r`'s write overlaps row `floor(r/2)`'s READ for every `r ≥ 1`: harmless in an ascending sequential loop, a genuine race across `par()`'s independently scheduled chunks. Measured: rows just after a thread-chunk boundary wrong by rel err up to **1.23** — wrong sign and magnitude, not bf16 noise — while every other row matched to ~1e-3. Fixed with a separate buffer; **the wrong proof is kept verbatim in the code comment**, because the shape of the error is the useful part. **And the golden gate PASSED the race.** It could not have done otherwise: the fixtures are batch 4 and the runtime tiles them 32× to fill a batch-128 design, so every "different" row is identical content and a wrong-ROW read is indistinguishable from a right-row read. A 13-distinct-sentence e2e run caught it at `1-cos` **0.44**. Filed as **T32** — the primary on-device accuracy gate is structurally blind to an entire bug class; treat a golden PASS as evidence about arithmetic, not indexing | M13 | done |
| [0071](0071-m13-nomic-shippable/TASK.md) | **`--prefix`, the C++ packer mirror, and the catalogue row — nomic becomes fetchable by name** — the prefix names a KEY in the container's own prompts table, never a literal, and the runtime **always prints which prefix it applied** (`prefix 'search_document' -> "search_document: " (container default -- no --prefix given)`) because a wrong prefix is a quality regression **no `1-cos` gate can catch** — both sides of such a check would use the same wrong prefix and agree perfectly. An unknown name refuses and lists the real options; found in review, `--prefix` given to a model with NO prompts table was silently ignored and now refuses, since a script sweeping one prefix across the catalogue *should* break rather than hand back vectors the caller believes are prefixed. BERT models untouched: no banner, `--embed` byte-identical with and without the flag. `prepare_model_nomic()` mirrors `pack_nomic()` including the verbatim float-literal copy and a new `add_gemm_b_concat2()` for the fc11\|fc12 fusion — **byte-identical on the first attempt**, sha256 `5bcb931a…` on both sides, no differing-byte diagnosis ever needed. **Cold path is the real gate and it passed**: checkpoint directory AND container moved aside, then one command did a genuine 546.9 MB fetch → sha256 pin → config cross-check → pack → design pick → encode, and the cold-built container came out byte-identical to the one moved aside | M13 | done |
| [0073](0073-m13-release-benchmarks/TASK.md) | **The 0.3.0 whole-catalogue benchmark sweep, done right on the third attempt** — five harness bugs fixed before a number could be trusted (`energy_compare.ps1` had no `-Model` param and had been unrunnable since 0038; the bridge couldn't load nomic; MTEB mislabelled every non-MiniLM result). Stages 1–2 were first measured **contaminated by a stray `find` process burning one core for 2.5 hours**, inflating the interleaved CPU ratio (bge-base 1.633×→2.456×) while leaving NPU throughput almost untouched — diagnosed, and `release_benchmark.ps1` gained a CPU-contention guard. Clean re-run: isolated throughput 951/494/211/60.8/166 seq/s (MiniLM/bge-small/bge-base/bge-large/nomic), **under 1.5% loss even with torch+ORT saturating all 12 cores**, energy 3.00–3.75× better than CPU, MTEB passes on all five (nomic first-ever, +0.09) | M13 | done |
| [0074](0074-m13-gemma-on-npu/TASK.md) | **EmbeddingGemma-300M's MQA `tile_n=16` tax escaped by zero-padding the fused QKV 1280→1536** (0055's own table already showed `tile_n=32` was legal; its verdict had quietly used the worst fusion option) — 97.7% of MACs now run on the array at **~133 seq/s** (4 lanes, 1.7% spread) vs the host-only path's 0.20 seq/s, **~665×**, `1-cos` 9.962e-06 against the host-only reference. Found and fixed a fail-open: `design_fits()` derived `qkv` N as `3·hidden`, which would have **rejected Gemma's own correct design**; `qkv_n` is now container data. A UTF-8 BOM from PowerShell's `Set-Content` corrupted exactly one row of a 520-line corpus and briefly looked like a per-row runtime bug. MTEB and CPU/energy ratios explicitly not run | M13 | done |
| [0075](0075-m13-arch1-measurement-harness/TASK.md) | **Teaches `run_mteb.py`/`compare_three.py`/`energy_compare.ps1` to reach arch=1 through `--embed`** rather than the BERT-shaped `--encode-file` bridge, since Gemma has no position table and a C++-only tokenizer. First MTEB run FAILED at mean −1.88 (worst −3.49) despite `1-cos` 9.96e-06 — diagnosed to `mteb` injecting its own task prompt **on top of** the container's, so CPU and NPU encoded different strings; fixed by making both sides apply the same mteb-selected prompt. Corrected run: **M8 gate PASSES at mean +0.00, worst −0.00** — the tightest MTEB agreement recorded (vs MiniLM +0.04, bge-small −0.03, nomic +0.09). Also cut CLAUDE.md's *Current state* from 1,345 to 375 lines, moving 1,027 lines verbatim to `docs/history.md`. Flags nomic's recorded +0.09 as now suspect (filed T36) | M13 | done |
| [0076](0076-m13-add-model/TASK.md) | **`npuembeddings add <repo> [<sha256>]` — finetunes run with no recompile.** Geometry is *derived* from a downloaded `config.json` (largest legal `tile_n` tried at 48/32/24/16/8), not matched against a hardcoded row; unpinned adds are allowed but warned **three times** (add, fetch, every run) per the user's explicit trust-model decision. `models/catalog.json` is a user catalogue merged after the six built-ins, forbidden from shadowing them. Proven on `TaylorAI/bge-micro-v2` (3-layer F16 finetune never seen before): **1-cos 5.507e-06** vs sentence-transformers, 652 seq/s. Found two real bugs invisible on the built-in six: the packer only read F32 (this checkpoint ships F16) and the golden-fixture guard's flat-fallback refused a model with simply no fixtures of its own | M13 | done |
| [0077](0077-m13-int8-gate/TASK.md) | **T20 opened: int8 builds, is bit-exact, and traces at 5.5–7.7× the bf16 datapath** (mean ~7.0×) on all four production MiniLM GEMM shapes, pre-tiled B, at production tile (64,64,48) — every outside estimate (1.33×–2.58×) was low because the real comparison is the idle MMAC unit vs the fp32 vector unit 0049 already characterised. int8×int8→int32 has no rounding in the reduction, so the gate is exact equality and it passed on every shape. Prices this as much less **end to end**: Amdahl bounds MiniLM ≤1.53×, EmbeddingGemma ≤1.72×, bge-large ≤2.20× (using bge-large's array share from 0045, 60.8%). No accuracy claim at all — that is left to 0078 | M13 | done |
| [0078](0078-m13-int8-accuracy/TASK.md) | **Naive W8A8 FAILS the 2e-03 gate (2.864e-03, corrected measurement); SmoothQuant α=0.5 on all four GEMMs via an explicit per-channel multiply (not a LayerNorm fold — BERT is post-LN, folding would corrupt the residual) PASSES at 1.166e-03 simulated / 1.417e-03 on hardware.** Two harness bugs fixed before trusting the numbers: attention GEMMs were being quantised despite a guard meant to skip them, and static calibration keyed by shape collapsed six layers' stats into one. Built and run on hardware: int8 container 58.68 MB vs bf16's 69.02 MB, **MTEB mean −0.03 (worst −0.13), indistinguishable from bf16's +0.04**, throughput 1086.7 vs 985.3 seq/s — **1.10× end to end** (MiniLM is the catalogue's worst case for this lever, 40.3% array share). *Its own §6 ("Not built": `Encoder::gemm()` still converts A to bf16) contradicts its own §4c ("BUILT AND RUNNING ON HARDWARE", with hardware numbers) — left as an unresolved internal inconsistency, not reconciled here* | M13 | done |
| [0079](0079-m13-int8-why-only-1.1x/TASK.md) | **Answers the user's own skepticism ("why is 1.1x all we get?") by measuring every link, not inferring.** bge-large gains **1.44×** (60.1→86.3 seq/s) against MiniLM's 1.10×, tracking array share of wall clock (**87%**, not 0045's 60.8%). Traces the gap directly: quantise/convert-A costs int8 **1.04× bf16's time** (15.95 vs 16.58 ms) — *not* more expensive, correcting 0078 §4c(2)'s reasoning — and the true bottleneck is **the C drain**: int32 C stays 4 bytes like fp32, so int8 moves zero fewer bytes even at 7× the arithmetic speed (dispatch 3084→1727 µs against a 961 µs prediction). Tested "quantise earlier": a static per-call-site activation scale **fails 57× worse** (6.693e-02) because padding rows dominate one tensor-wide scale. **bge-large int8 FAILS the 1-cos gate at every α** (best 3.127e-03, 1.56× over budget; MTEB not run). Recommends against downloading pre-quantised ONNX/GGUF models. The file carries no `Status`/`Milestone` header field | M13 | done |
| [0080](0080-m13-int8-traffic-bound/TASK.md) | **int8 flips the GEMM cost model back to traffic-bound, un-retiring 0048's "bytes are free" verdict** — fitting both cost models to the four production shapes shows 0010's traffic model wins under int8 (R² 0.987, worst 5.3%) where 0048's iteration model wins under bf16 (R² 0.994); confirmed by building `k=128` (bit-exact, halves k-iterations) which bought **0.989×**, refuting the iteration model outright for int8. C is 61.5–61.6% of traffic on three of four shapes because int32 C is 4 bytes exactly like fp32 was. Built `narrow_i32_bf16.cc` and a `--sim-c-bf16` flag that predicts a narrowed-C design's accuracy from the host dequantiser alone — predicted MiniLM 1.161e-03, the built design then measured **exactly that**. Narrowing C to bf16 cut traffic 207.5→151.0 MB (−27%) across the four shapes and raised MiniLM 1104.2→**1204.7** seq/s, bge-large 86.3→**93.1** (4 lanes). Found and fixed a live bug: `design_fits()` matched only `(op,K,N)`, so `serve`/`embed` handed an int8 container a bf16 design set that died on `b_layout_hash` mismatch — fifth instance of a selector matching on too little. bge-large int8 still fails `1-cos` (4.436e-03, still the α=0.3 container) | M13 | done |
| [0081](0081-m13-int8-everywhere/TASK.md) | **`tile_n = 64` for bge-large int8 — a width CLAUDE.md's own geometry section had said was illegal, because it assumed bf16's L1 budget** — int8 halves operand bytes, so `(64,64,64)` fits at 49,152 of 63 KB where bf16's identical tile needs 65,536; bit-exact, and cuts bge-large's four production shapes 35,927→**26,292 µs** (1.366×). Also found the release benchmark's "everything else" bucket was double-counting the GELU/softmax/LayerNorm lines printed above it, inflating bge-large's unexplained cost from 12.9% to a phantom 24.4%. The corrected breakdown says **the array is only 30.4% of a bge-large encode** — an infinitely fast NPU caps the whole thing at 1.44× — and the other 69.6% is host memory traffic (four to six streaming passes over 33.5–134 MB tensors between GEMMs, ~17 GB/encode). All six models now have int8 containers; five pass `1-cos` (MiniLM 1.161e-03, bge-small 6.385e-04, bge-base 1.778e-03, nomic 1.098e-03, EmbeddingGemma 1.902e-03 differential); **bge-large's best is 2.968e-03, still a `1-cos` FAIL but MTEB PASSES** (mean −0.05, worst −0.18) at α=0.5/tile_n=64. nomic and Gemma needed their own calibration oracles (5 and 7 GEMM sites/layer against the packer's 4) and Gemma needed quantisation added to its separate `GemmaNpuEncoder`. Sets direction for 0082: fuse the host epilogue chain | M13 | done |
| [0082](0082-m13-fused-ffn-epilogue/TASK.md) | **T37: fusing the host's two multi-pass GEMM-epilogue chains into one L1-resident pass, so the 134 MB `up` tensor (268 MB gated) is never materialised** — first attempt used scalar inner loops and was NOT bit-identical (`1-cos` 1.161e-03→1.180e-03) because `_mm256_fmadd_ps` rounds once where scalar `a*b+c` rounds twice; fixed by matching the exact intrinsics, giving byte-for-byte identical `--embed` output on all four gate models. Covers plain GELU (arch 0), gated SwiGLU (arch 2, nomic — the bigger win since the unmaterialised tensor is 2× wide) and gated GeGLU (arch 1, EmbeddingGemma, wired through the same parametrised helper). Measured gains vs the unfused path (later shown ~4% low by 0084's 3-run rechecks): MiniLM 1.394×, bge-small 1.417×, bge-base 1.418×, bge-large 1.428×, **nomic 1.482×** (largest, matching its wider intermediate); vs the bf16 baseline, 1.65×–**2.66×** (bge-large). Also fused the LayerNorm/residual/quantise chain (8 passes→4), which caught a real slot-vs-site off-by-one segfault via the byte-comparison harness already in place. Found the host runs at **56–70 GB/s**, a new project constant distinct from the NPU shim's ~28 GB/s (0080) — using the wrong one had briefly made a correct traffic model look like it failed | M13 | done |
| [0083](0083-m13-join-port-budget/TASK.md) | **T28: the register itself was stale, and a real port-budget sweep separates L1 exhaustion from DMA channel exhaustion for the first time** — before measuring anything, a "new" derivation was filed under T28 that 0062 had already built and passed on hardware three days earlier (rel_fro 1.726e-03); the register never named 0062, so its reader re-derived on paper what was already in the tree — corrected same commit, the third instance of this failure class and the first where the register itself misled. Built `join_port_budget_probe.py` at 2 KB tiles (vs production's 6 KB) to make L1 irrelevant so only topology can fail: **2–5 sources PASS bit-exact, 6 FAILS** (a mem tile serving a join plus an out-relay has 0 of 6 input channels left for the relay hop), **7–8 FAIL** (the join itself exceeds 6 ports) — refining 0057's "a flat 8-way join cannot express" into an exact number: 5 sources with a co-resident relay, 6 for a bare join. Estimated (not built) that a streaming k-block relay, rather than 0062's one-shot gather, fits production's `(k=64, N_DOWN=48)` tile at 47,104 of 63 KB with 17,408 B spare — the item 0062 left as "not designed or estimated." Fourth instance of the marker-specificity cache fail-open (marker keyed on a value that varies across the sweep, so every N silently ran the N=2 binary) | M13 | done |
| [0084](0084-m13-host-isa-and-repeats/TASK.md) | **Re-measuring after 0082's fusion inverts the priority list, and two of the task's own conclusions had to be retracted same day** — bge-large's breakdown post-fusion: wall 1465.73→**1087.57 ms**, array back to the largest single item at **40.4%** (was 30.4% pre-fusion), attention now second at **19.1%**, untouched by anything so far. First claimed attention was compute-bound (497 GFLOP/s / 88% of AVX2 peak) via a grep that missed that `qk_impl`/`av_impl` are hand-written `_mm256_*` intrinsics, not compiler-vectorised — corrected in place. Rebuilt at `/arch:AVX512`: all five gates reproduce exactly, but throughput is unchanged (1.00–1.014×) — and the first explanation ("Zen 5 double-pumps 256-bit") was ALSO wrong and retracted: a compiler flag cannot widen hand-written 256-bit intrinsics regardless of hardware width, so **attention at 512-bit is untested, not tested-and-failed** (tested next in 0086). Re-ran 0082's single-run A/B three times per arm: ratios held within 3% but the published fused absolutes ran **~4% low** (MiniLM 1599.6 vs a 3-run 1672.0), and 0082's own "~4% spread" claim was itself wrong — one of its two bge-large readings was contended; true quiet-machine spread is **under 0.5%** | M13 | done |
| [0085](0085-m13-release-sweep/TASK.md) | **The 0.4.0 sweep: twelve rows (six models × two datapaths), one session, one protocol, three runs per throughput cell** — int8/bf16 gains MiniLM 1.71×, bge-small 1.71×, bge-base 1.94×, **bge-large 2.39×** (best), nomic 1.95×, EmbeddingGemma 1.14×; spread **under 0.6%** on five of six rows, settling 0084's "~4%" as a one-off contended reading. All twelve rows PASS MTEB; the two accuracy gates rank models differently — bge-small has the best `1-cos` (6.385e-04) and the worst MTEB delta (−0.09/−0.41), bge-large has the only `1-cos` FAIL (2.968e-03) and a comfortable MTEB PASS reproducing 0081 digit-for-digit. **EmbeddingGemma loses to torch on the CPU (0.88×)**, not noise — arch=1 runs RMSNorm×97/RoPE/MQA on the host with only four GEMMs/layer on the array. Found and fixed three plumbing bugs: a PowerShell subexpression-doesn't-splat bug that silently dropped `--cpu-model` and cost all six int8 MTEB rows two hours in; a positional-vs-named splat bug that broke the energy stage; and `energy_compare.ps1` passing a `.npue` container name where a HuggingFace checkpoint directory was needed, which died silently and reported energy deltas between **−0.8 and 1.2 J/1000 seq** — now guarded by a precondition refusal. Also fixed `make_release.ps1` hardcoding an old sweep path, which would have shipped stale numbers silently | M13 | done |
| [0086](0086-m13-attention-host-isa/TASK.md) | **Both 512-bit attention paths built, neither pays, and the reframing kills 0084's "compute-bound" premise** — splitting `t_attn` into QK^T and A·V (previously conflated) shows them ~50/50 (bge-large 98.7/105.2 ms). A·V at 512-bit is bit-identical by construction (per-output-element accumulation never reassociates) and gains **nothing** (207.40→207.83 ms) — direct evidence attention is memory-bound, not compute-bound, contra 0084's retracted "double-pumping" guess. QK^T at 512-bit measured **1.33×** in an isolated microbenchmark (`_mm512_reduce_add_ps` replacing `hsum256`'s shuffle chain) but built in situ it went 98.7→**106.6 ms — slower**, because the microbenchmark kept `q`/`k` L1-resident across 20,000 reps while the real kernel streams K out of `qkvbuf` with large row strides — the microbenchmark inverted the sign of its own result. Concludes VNNI's value on attention is not arithmetic (both widenings refute compute-boundedness) but traffic — int8 Q/K/V would be ~2× fewer bytes for QK^T, ~1.33× for A·V, sized at **~6% of the encode** if quantisation is free; not built. Also fixed a same-day regression: `CMakeLists.txt` defaulted `NPUE_HOST_ARCH` to AVX512, contradicting 0084's own stated AVX2 default — reset, and both 512-bit paths kept in source behind `__AVX512F__` as the negative evidence | M13 | done |
| [0087](0087-m13-relay-production-width/TASK.md) | **T28's streaming relay at production width: 0083's "fits on paper" L1 estimate was wrong by exactly 8,192 bytes, and once fixed the design compiles and then hangs** — 0083 §4 forgot the 2-hop design needs fp32 output (12,288 B, not the assumed 6,144 bf16) plus the 2,048 B two-worker program stack (CLAUDE.md trap 3); actual build at `N_DOWN=48` needs 69,632 of 65,536 and fails allocation. Narrowing the output to bf16 (0080's own lever, applied here for address space rather than bandwidth) gets to 63,488/65,536 and the design **compiles** — then returns `ERT_CMD_STATE_TIMEOUT`. A 2×2 isolation (`N_DOWN` 16/48 × fp32/bf16 output) proves width is not the cause: `N_DOWN=16` with bf16 output hangs at the exact geometry that passes at fp32 (reproducing 0062's control, rel_fro 1.726e-03). A further discriminator (`NPUE_BF16_COPY=1`, a plain bf16→bf16 copy with no narrowing kernel at all) **still hangs**, refuting the kernel `narrow_f32_bf16.cc` itself as the cause. Leading remaining suspect: this probe's core→shim hop is a bare `forward()` with no explicit tile (the working bf16 GEMM drain uses a `join()`), echoing 0054's known "bare point-to-point ObjectFifo compiles but hangs with no diagnostic" class. Fifth instance of the marker-specificity cache fail-open (this time on `N_DOWN`). Even fixed, this is a 4-column, one-k-block toy, not `ffn_down` at K=1536/8 columns with an MMAC-accelerated relay | M13 | partial |
| [0088](0088-references-external/TASK.md) | **External XDNA/NPU repo survey — `research/references-external.md`**, eight repos read from local clones at pinned commits, not from READMEs. **XDNA2 is nearly absent from this whole ecosystem**: six of eight are XDNA1, one has a single unshipped `aie2p` FFT probe, and the one repo on our own generation (`npuhalo`, Strix Halo, 48 AIE2P tiles) drives it only through FastFlowLM, never IRON. `npuhalo` measures **−16.5%** iGPU decode cost from concurrent NPU DMA on shared XDNA2 memory, with the NPU itself barely slowing — the DMA wins contention and the GPU pays. `Meeting-Ops-UC1-OSS`'s "NPU-accelerated" MiniLM embedding service configures `DmlExecutionProvider` (Windows DirectML) on a Linux target and its Whisper "220x" traces to a self-described *"mock XCLBIN"* with `np.random` inputs — rules 1/6's failure mode, found in someone else's shipped "complete" feature. Two repos' GitHub-reported licence (`NOASSERTION`) was wrong; three are AGPLv3, flagged do-not-vendor | M13 | done |
| [0089](0089-t15-task-index-backfill/TASK.md) | **T15 closed — and the debt it named was 21 rows, not six.** Reading all 21 missing `TASK.md` files (0038–0043, plus 0073–0087 that T15 never knew about) from their own text, not from `CLAUDE.md`'s summary, per rule 3. Found two stale claims in the summary docs while doing it, left unreconciled by design: CLAUDE.md credits [`0043`](0043-m9-attention-geometry/TASK.md) with a "not worth the fight" cost/benefit verdict its own `## Results` section (`(filled in below)`) never reaches — only the structural `cols ≤ 4` argument is backed by that file; and CLAUDE.md's "1.10× on MiniLM, quantisation costs more than bf16 conversion" restates a [`0079`](0079-m13-int8-why-only-1.1x/TASK.md) hypothesis that 0079 itself measured and refuted (1.04×, not more expensive — the real cost is the C drain), now further superseded by [`0085`](0085-m13-release-sweep/TASK.md)'s measured 1.71×/2.39×. `tools/check_register.py` passes before and after | none | done |
| [0090](0090-t5-dma-compression/TASK.md) | **T5 closed — DMA compression cannot pay, and the reason is silicon.** AIE2P's **shim DMA has no compression hardware at all** (`.Compression = XAIE_FEATURE_UNAVAILABLE` in the vendor's own register table; mem-tile and compute-tile DMA have it), and every byte 0080's int8 traffic model prices crosses the shim — the only door from DRAM into the fabric. Control reproduced first (**1.391x on `arange`**, sha256-matched to the example's golden). Best case granting the lever everything — full ratio on B, shim gap ignored — is **5.8%** across the four production dispatches, before decompression cost. The real-weight ratio stays genuinely unmeasured: the attempt was blocked by a **newly-found `iron.jit` cache bug** (the cache key never inspects a generator's module globals, so a resized BD silently re-ran the control **even with `use_cache=False`**; `.as_mlir()` sees the change, isolating it to the compile layer) — the **sixth** cache-staleness fail-open here and the first inside IRON rather than our own markers. Proxy signal: real weights carry 0.23-3.13% zero bytes against `arange`'s 51.7% | research | done |
| [0091](0091-t7-t8-gelu-poly/TASK.md) | **T7 and T8 closed — `gelu_poly.cc` off the emulated-multiply idiom, and note 0007's error table corrected.** T7's fix (`accum::from_vector` for both widen and narrow) cut the shipped kernel **652 -> 580 `vmul.f`/`vadd.f`, -11.0%**; a **third, previously-unlisted instance** in the fp32 GEMM epilogue turned out to be a **dead multiply-by-one** and was deleted outright (648 -> 580). **No shipping kernel carried the pattern** — `narrow_f32_bf16.cc` and 0062's `gelu_epilogue_3072_f32_to_bf16` were already correct — but it is live in T28's `ffn_down_hop_matmul_g2.cc`, reported rather than fixed (file in active use). 0045's "34 emulated ops/64 elements" re-measures as **21** today: same conclusion, toolchain drift, recorded not reconciled. T8's degree-7 **refit** (not a truncation) saves a further **-12.4%**, and numpy reproduction found **note 0007 §3.2's 3.6e-04/5.9e-04 does not reproduce under three metrics** and appears to omit bf16 output rounding — in the metric that gates shipping, degree 7 and 8 are **2.503e-03 vs 2.494e-03, 0.36% apart**, because the bf16 rounding and not the polynomial sets the error. 0007's direction holds, for a different reason than it gave | research | done |
| [0092](0092-t28-relay-bf16-output/TASK.md) | **T28's bf16-output hang found and fixed — it was never on the chip.** Five on-chip suspects refuted with hardware evidence (narrowing kernel, relay stack, fifo depth, mem-tile contention, and decisively *all upstream compute*: a kernel writing a hardcoded constant still hung). Then a diff of the emitted `aie.dma_bd` ops between the passing fp32 build and the hanging bf16 build of the **identical** design found it — the design's host-facing `Y_ty` was **hardcoded fp32** while `A_ty`/`B_ty` two lines above were conditional, so the shim DMA moved 4,096 B against a 2,048 B host buffer and waited forever. One line. A second one-line bug (`Y_out` double-buffered when narrowed, +5,376 B over L1) surfaced on the next attempt. **`N_DOWN=48`, production tile width, now PASSES at rel_fro 2.510e-03** against fp64, 3e-2 tolerance; 0062's fp32 control reproduced at 1.726e-03. Also: 0087 §4's `NPUE_BF16_COPY` discriminator was **invalid** — `arg_types` shapes only the MLIR call site, so it fed a `float*` kernel a 2,048 B buffer (objdump-confirmed 2x overflow); its conclusion survived a corrected retest. Still no trace, so **correctness only, no performance claim** | M13 (T28) | done |
| [0094](0094-t32-golden-gate-rows/TASK.md) | **T32 closed — the gate compared 4 of 128 output rows, and all-rows alone still was not enough.** Alongside T32's own "indistinguishable copies" hole, `want` was read at a fixed 4 rows and the loop stopped at `b < kGoldenBatch` regardless of batch, so 124 of 128 rows were **never read** — the direct explanation for how 0070's `swiglu_cpu()` race passed. Fixed both: the loop covers all rows (and says so), and the four goldens are tiled with a per-copy rotation so every row's content is unique. **Five known-bad artifacts**: corruption PASSES the old gate / FAILS the new; a same-slot cross-copy swap **PASSES with plain tiling even when all rows are compared** — the row that justifies the rotation — and FAILS with it; NaN still FAILS. **No published number moved** (MiniLM 4.473e-03, bge-base 4.297e-03, bge-large 3.763e-03, nomic 6.119e-03), so rows 4+ were always as good as 0-3; nobody had checked. Side finding: a `+5.0f` corruption moved `rel_fro` but left `1-cos` untouched — the metric assumes unit norm | M13 (T32) | done |
| [0095](0095-t6-t10-probes/TASK.md) | **T6 and T10 answered from static evidence, no hardware.** T6: `aie_stream=` **does** free a channel — core tile out 1/2 -> 0/2, and the `aie.dma_start(MM2S,...)` block is *absent*, not idle — but only on a core endpoint (a shim has no program counter, and marking a consumer that still `.acquire()`s is a compiler error), while 0046/0047 pinned the real scarcity on **mem-tile inputs**. B-reuse stays closed, now by measurement. T10: `aie::exp2` is **one inlined `vexp2`** for a whole 64-wide row (291 instructions) against `exp2_poly`'s unrolled Horner chain (765, **2.6x**), neither emitting a call — but its accuracy is **1.711e-02 against the polynomial's 7.109e-03, 2.4x worse**, so `exp2_poly` stays and the trade is stated. Under `conv_even` the polynomial would be 1.92x better again — a fifth instance of the `floor`-bias pattern. Bonus: `CURRENT_STATUS.md` called the `exp2_poly` softmax-composition bug "never diagnosed"; it was diagnosed and fixed in 0030 (stack 0xD00 too small), corrected in place | research | done |
| [0096](0096-t26-numerical-mechanism/TASK.md) | **T26: a host model built to explain the 6.6x anomaly FAILED ITS OWN CONTROL, and that is the result.** numpy/fp64, calibrated to 0008's real-hardware bfp16 fit (7 bits/element, block 8, cross-checked against `aie_api`'s `type_bits<bfp16ebs8>`) and structurally faithful to 0056's two narrowing sites. Its fp32C/bf16C ratio stays **pinned at 0.99-1.00 across four stages** — and eight, under 300-trial averaging — against hardware's 0.99 -> 1.35 -> 1.46 -> **1.67**, at every mantissa calibration from 3 to 9 bits. Per the control rule the ablations are not treated as mechanism evidence, but the failure **rules out** "narrow position applied to independent per-block bfp16 noise" as sufficient; both paths show near-zero error mean and |corr| < 1.3e-03. Real weights through the real 6-layer encoder give **1.195x** where Gaussian gives 1.00 — a direction, an order of magnitude short. Closes 0056's non-goal: the shipping plain-bf16 path *does* carry an effect, of **opposite sign and trend**. Next step is kernel source or a device ablation, not another host model | research | done |
| [0097](0097-t18-t21-t4-measurements/TASK.md) | **Three measurement threads closed on an idle array.** **T18**: the 8-10% probe-vs-bench gap **did not reproduce** - probe 3,034.4 us against bench's `wait (hardware)` 3,028.3 us, **0.2% apart**, spread under 0.3%; both harnesses call the same `dispatch_only()`/`run::wait()`, so there was never a definitional difference. Quote `--bench`'s `wait (hardware)`. **T21**: within one model, three of four shapes are **already at their legal `n` ceiling** at 48; only `ffn_up` can reach 64 (`4*hidden` always divides 512, the others usually do not), measured 1.139x on that shape and **1.043x on the four-shape sum** - and **exactly zero on bge-large**, where all four already divide 512, which is why 0081 could move it wholesale. **T4**: 0043's empty Results filled from its own commands - the attention-capable geometry costs the projections **2.229x**, decomposing multiplicatively as 1.289x tile size and 1.729x column count (1.289*1.729 = 2.228). CLAUDE.md's long-unsupported "not worth the fight" verdict is now earned. Spun off **T38**: mem-tile padding would cut that to 1.289x, and `xaie2pgbl_reginit.c:1667` confirms the mem tile has `.Padding` where the compute tile and shim do not | M13 | done |
| [0099](0099-t26-rounding-ablation/TASK.md) | **T26 answered: the 6.6x anomaly was a mlir-aie 1.3.4 artifact, and the 1.4.2 upgrade already fixed it.** The poison ablation ran and is an **exact null** - cold and warmed fp32-C bit-identical per seed to full float64 (9.44211e-03), with bf16-C 1.6% *worse* rather than 6.6x better. That null IS the confirmation: a fresh rebuild of the **same config hash** `333c4d33` now contains the `crrnd` save/set/restore triple the checked-in 2026-08-20 artifact lacked (0 vs 3; instruction streams genuinely differ, not a disassembler effect - `mov r29, p0` where today has `mov r22, crrnd`). And the dates settle it: the artifacts landed in `efd8c76` at 12:15:45, the 1.3.4 -> 1.4.2 migration in `42da31d` at 16:05:34 the **same day**, ancestry-confirmed - so 0052/0053/0056, every measurement that ever saw the anomaly, ran on 1.3.4 where no rounding control was emitted. `t26_chain_probe.py` unmodified no longer reproduces 0056's 0.99->1.67 divergence. Ablation design note: `poison` places a discarded `narrow()` **after** the real release, so C stays fp32 and cold/warmed are the same xclbin in one process - avoiding the cross-context confound 0098's literal proposal had. **Consequence: T23's whole accuracy case is toolchain-stale and must be re-measured** | research | done |
| [0101](0101-t23-bfp16-gates-on-1.4.2/TASK.md) | **T23 re-measured on 1.4.2 after T26 showed its whole evidence base was 1.3.4-era.** Three configs x two models, plain bf16 as a control that reproduced twice to the digit. **The 6.6x is gone**: fp32-C moved 2.395e-03 -> 3.368e-04 (~7x) while bf16-C barely moved (3.615e-04 -> 3.406e-04) - exactly the asymmetry T26's mechanism predicts, since fp32-C was the one stuck at `floor`. **0035's `--emulate-bfp16` retirement is partly overturned**: it now passes `1-cos` comfortably on both models, but **fails MTEB on bge-base** (worst −0.58 against the −0.5 line), so it stands for that model. **The two gates disagree in DIRECTION** - `1-cos` ranks fp32-C better, MTEB ranks bf16-C better, same CPU baseline - which is new and is the strongest case yet for keeping both. Recommendation: **bfp16 + bf16-C clears both gates on both models and is also faster** (2.04-2.29x array GEMM), not for the refuted 6.6x reason but because it is the one that passes. Caught and corrected mid-task: the first pass reported MTEB verdicts **the gate never issued** (all four printed `NO GATE ... cpu side(s) not run`); re-run with `--sides cpu,npu` the real gate confirmed every cell to within 0.02 points - right arithmetic, still not a result | research | done |
| [0103](0103-t23-bfp16-all-models/TASK.md) | **bfp16 across the whole catalogue: five of six pass, and the one failure misses by 0.001.** Real gate verdicts (`--sides cpu,npu` in one invocation) on the adoption candidate bfp16+bf16-C: MiniLM +0.12/−0.07, bge-base −0.06/−0.19, **bge-large +0.13/−0.01** (widest model, tightest delta), nomic +0.01/−0.25, gemma +0.16/−0.02 — **all PASS, across three architectures**. **bge-small FAILs at −0.5010** against the −0.5 line and it is **not noise**: reproduced bit-identically (`cpu 0.4862581899`, `npu 0.4812477913`), both sides deterministic. An initial "coin flip" reading was wrong and is corrected — the wide swings are variance *between* models, not *within* a measurement. Two findings beyond the verdicts: **`TwentyNewsgroupsClustering` is the worst task in all ten cells** (+0.89 to −0.58) while the other four stay inside ±0.12, so the gate is in practice a test on clustering; and **bfp16 does not degrade with width the way int8 does** (flat 2.2-3.4e-04 vs int8's 4.6× spread), contradicting an assumption carried since 0081. Speed on bge-large: array GEMM 19,099 -> 9,776 us, **1.95x** | M13 (T23) | done |
| [0106](0106-toolchain-provenance/TASK.md) | **T39 closed at the hour it was priced at: `toolchain.json` beside `design.json`.** Three strings (`mlir_aie_version`, `peano_version`, `mlir_aie_git_head`) written by both export tools; the runtime prints them beside `datapath`, and a design without one reads **`UNRECORDED`** rather than a guess — 0104's absent-is-not-a-value discipline. **Deliberate departure from note 0009**: it also asked `pack_npue.py` to copy the string into the `.npue` header, which it must not — a container is packed by a tool that never invokes mlir-aie, and the design↔container relation is many-to-many (0104 gave MiniLM and bge-small *different* designs at the *same* geometry). Degradation proved against the **failing** case: pointed at a non-git dir the export still succeeds and records `"unavailable"`. Two failures kept: the coordinator's own re-export script printed `ALL SIX RE-EXPORTED` while two had failed, because `$ErrorActionPreference = 'Stop'` **does not cover a native command's exit code** and an `[XRT] ERROR: ... device has been removed` sailed past it; and that same transient removal produced a subtly wrong build the exporter's **own** identity guard refused (82 differing bytes vs a threshold of 80, not reproducible — a third run gave 68–73 and exported cleanly). All six shipping designs re-exported | research | done |
| [0132](0132-t51-tail-gate/TASK.md) | **T51 step 3: the release sweep has a gate that sees a tail.** `tools/verify_tail.py` (stdlib-only, runs against a cold zip via --exe/--root) gates **p99 of per-text 1-cos** over 0129's 224 texts against per-model baselines in `reference/tail/` -- ceiling = max(2e-3, 2x measured p99), a ratchet: tight where the model is tight (bge-small p99 1.16e-05), honest where it is not (bge-large 6.63e-03 with an explicit T51 waiver note in the baseline file). Refuses to compare across datapaths (0129: the tail is input-determined but the LEVEL is datapath-determined); unbaselined model = FAIL. All six models baselined; bge-large reproduces 0122/0129 to the digit (same worst five words); **negative control proven** (tightened ceiling -> FAIL, exit 1). Wired into `release_benchmark.ps1` as a skippable `tail` stage; `tail_pass` lands in sweep.json. nomic reads a LEVEL not a tail (median 1.08e-03, max/med 1.5x -- consistent with its known 1-cos 1.402e-03); MiniLM's worst input sits 8% under its ceiling, the likely first alarm if anything drifts | 0.5.0 P6 | done |
| [0133](0133-t52-cpp-port/TASK.md) | **T52 step 2: the C++ Unigram tokenizer, byte-identical on every axis, first run.** C++ generator blob sha256-IDENTICAL to the Python generator's (5,318,988 B -- which also dissolved the f64 storage worry: json_min's UCRT strtod is correctly-rounded, proven by identity over 250,002 raw-f64 scores, 65,856 of them not f32-exact); C++ CLI vs HuggingFace **343/343**; C++ CLI vs the Python reference **343/343**; zero /W3 warnings. New: `tokenizer_xlmr.{hpp,cpp}` (f64 Viterbi, HF grapheme quirk, Rust White_Space, in-place Darts walk, `from_table_bytes` ready for the container), `xlmr_tokenizer_gen.{hpp,cpp}` (every fail-closed guard in the Python's order + strict base64), `tokenizer_xlmr_cli` as a real CMake target (the gemma CLI never had one -- 0061 hand-built it), generated `xlmr_unicode_tables.hpp` (UCD 15.1.0, the bert_unicode_tables precedent). Deferred, recorded: added-tokens splitter decision; arch-3 wiring (main.cpp/npue_pack/hub) -- the only remaining piece of T52 | 0.5.0 P4 | done |
| [0146](0146-docs-catch-up-decoder/TASK.md) | **The documents catch up to the decoder workstream, and the public repo is pushed — release discipline, no release.** Three days of work (0143–0145) had landed as task logs and traps only: `docs/CURRENT_STATUS.md` and `README.md` had not moved since 0.5.0 and **`research/OPEN-THREADS.md` was byte-identical to its 0.5.0 state**, which is the exact failure rule 3 exists to prevent, one sweep after the rule grew a checker. **Four threads filed** — [T55](../research/OPEN-THREADS.md#t55) (is there an NPU case for decode at all? energy and prefill, both unmeasured), [T56](../research/OPEN-THREADS.md#t56) (24 cores and fusion each pay and do not compose), [T57](../research/OPEN-THREADS.md#t57) (gte's CPU/energy cells: harness repaired, run not taken), [T58](../research/OPEN-THREADS.md#t58) (the host engine's unexplained quality bug) — live count **5 → 9**, and `check_register.py` took 0143 off its unreferenced list as a result. New **§11** in CURRENT_STATUS states the decoder work is a research direction, that none of its code is in this repository, and that nothing is vendored from FastFlowLM (rule 4). Also fixed 0143's one named leftover: the release skill's push block now checks which branch `repo/` is on and says that a rejected push must **never** be answered with `git pull`. Sync PASS, 1879 files, no dead links. **No version bump, no zip, no release note, no hardware run** — the shipped product is bit-for-bit 0.5.0 | docs | done |
| [0145](0145-granite-npu-gemv/TASK.md) | **granite's q4nx weights on the array: a W4A16 GEMV kernel — and a 24-thread AVX2 CPU is 2.4× FASTER.** All 8 projection shapes (q, k, v, o, gate, up, down, lm_head) match a host reference built from the same bytes at cosine **1.00000000**, exact under a one-hot activation — ~99% of the model's weight traffic. RMSNorm, RoPE, SwiGLU and attention run too; a granite layer fuses to **three dispatches for 1.40×**, the memtile leg reaches **24 of 32 cores for 2.04×**, and a hybrid NPU+CPU split beats the best single device by **1.37×**. **The task refuted two of its own claims and kept both**: a ~20-line null kernel measured the array's weight path at **46.3 GB/s against the CPU's 47.0**, killing "even a perfect NPU design loses" and showing the kernel is compute-bound by 2.3× (needs ~18 cores, has 8); and running both devices at once showed **~50 GB/s is a per-agent limit, not the platform's**. Seven traps came out of it into CLAUDE.md, including a **128-byte shim DMA that silently delivers zeros**, `static` vs `inline` deciding whether entry-point copies merge (11,648 → 2,912 B), and a float32 cosine that failed a bit-identical vector. Open: [T55](../research/OPEN-THREADS.md#t55), [T56](../research/OPEN-THREADS.md#t56) | decoder | done |
| [0144](0144-granite-q4nx/TASK.md) | **Granite 4.2-3B converts to q4nx and the fold is bit-exact — and FLM's shipped designs cannot run it.** `granite-4.2-3b` is dense `GraniteForCausalLM` whose GGUF tensor set is **exactly Llama's dense subset**; the difference is four scalar multipliers, and **all four fold exactly into the weights**, which is what the port rests on. The model generates coherently through an engine written for it, with no C++ added to FastFlowLM. **[T54](../research/CLOSED-THREADS.md#t54) answered on hardware**: `llama_npu.dll` whitelists `hidden_size` to {2048, 3072, 4096} and granite is 2560, so the engine refuses before it reaches an xclbin — which is what sent 0145 to write a design instead of borrowing one. [T53](../research/CLOSED-THREADS.md#t53) (the nibble order, undetermined by one bit) filed and answered the same day. Wrote [note 0010](../research/notes/0010-q4nx-format.md) on the container format. Left open: the host engine's output-quality bug, [T58](../research/OPEN-THREADS.md#t58) | decoder | conversion done, runtime blocked |
| [0143](0143-gte-cpu-energy/TASK.md) | **INTERRUPTED deliberately — the harness is repaired and verified, the measurement is NOT taken, and nothing here claims a gte number.** The gte CPU reference arm was broken by `transformers` 5.15 instantiating `trust_remote_code` modules on the **meta device**, so every `persistent=False` buffer comes back as uninitialised memory — including `embeddings.position_ids`, where in-bounds garbage silently encodes the **wrong positions** and returns a plausible vector. Fixed by detecting gte from the container's own `arch` field, forcing fp32 at construction, and importing `repair_rotary` from the one place it is written down rather than restating it; `release_benchmark.ps1`'s opt-outs are gone. Second half: **`Claude-Session:` links were stripped from every public commit** — a URL that resolves for exactly one person, published to everyone else — and the rewrite taught two things the hard way: a release had been pushed to a `v0.4.0` branch where `main` never received it, and answering the rejected push with `git pull` produced **23 files in conflict on a tree neither side had touched**, because it merges the pre-rewrite history back in. **A rewrite is finished by force-pushing it.** The two empty cells are now [T57](../research/OPEN-THREADS.md#t57) | 0.5.0 P10 | harness done, measurement not run |
| [0142](0142-release-050/TASK.md) | **Cutting 0.5.0: the cold zip fetches, verifies, C++-packs and runs the seventh model BY ITSELF -- and the data region proves it.** Docs updated (README 7-model table with the p99-tail column; CURRENT_STATUS 0.5.0 table + a s9 that no longer ranks eight closed threads; release note from task logs); sync_public_repo PASS (1841 files, the 0.4.0-era failures were uncommitted-task links, self-resolved); repo/ committed locally, push left for the user. Zip 0.82 MB with the SAME six design sets as 0.4.0 -- the seventh model needing no new array design IS the release. Cold test: `embed gte-multilingual-base` from nothing fetched 582.5 MB + the new kFilesGte set, verified pins, C++-packed (incl. C++ XLMRTOK1 generation) and ran on the NPU -- after HF cut the unauthenticated stream three times (207/278/344 MB, loud exit 2 each time, the refusal working). **`npue_data_hash` on the container the cold zip built itself: byte-equal to the validated one, exit 0.** Semantic gate 7/7 and tail gate 7/7 against the cold exe+root; root correct in all four layouts (the 0051 climb bug absent). Six containers seeded from repo models/ per the user's call -- already-verified bytes beat a 3.7 GB re-download. Tag + gh release + push: the user's | 0.5.0 P9 | done |
| [0141](0141-release-sweep-050/TASK.md) | **The 0.5.0 sweep: seven models, one protocol, the tail gate's first release outing -- and the machine's loudest contender was this agent session itself.** The CPU-quiet gate refused three times, naming a bash shell burning 0.25 cores -- a leftover subagent poller in this session's own shell; killed rather than excluded (an exclusion would have tainted the ratios silently). Attempt 4 clean at 2.7% CPU: six carried models reproduce 0.4.0 within +-1% (the regression check IS the flat column); **gte lands at 255 seq/s** with 1-cos 4.608e-04 and p99 tail 5.2e-04, CPU-ratio/energy deliberately opt-out per row (the trust_remote_code repairs make a CPU arm unusable). Tail gate PASS on all seven against recorded ceilings, bge-large inside its waiver as designed. int8 rows ran clean this time (interleaved no longer fails them). gemma's NPU energy arm produced a degenerate differential (dt~0 between low/high -- 0085's exact shape); DISCARDED, 0.4.0's 3.72x carried with provenance. Energy ratios moved 60% with the CPU side between sweeps -- the indicative-only caveat, now with its best example | 0.5.0 P9 | done |
| [0140](0140-gte-longseq/TASK.md) | **gte at long sequences: the NTK reading verified where it matters, and the seq-256/512 sets serve the 7th model unchanged.** Separate s512 container per the nomic precedent (main container + C++ hub pack stay byte-identical); seq goldens at 256/512 via 0116's --seq (two-oracle agreement 6.7e-08). **The wrong-theta discriminator GROWS with position exactly as rotation-angle arithmetic predicts**: plain-20000 misses layer 0 by 1.9e-02 at 18 tokens (0134) and **4.2e-02 at 512**, while the oracle holds 1e-05 out to position 511 -- the NTK reading is verified at both ends of its range. Hardware: both long-seq design sets serve gte at **1-cos 1.9e-04..4.3e-04** over 17-506 tokens (incl. a 506-token Norwegian text), adopted datapath reported, and a 227-token input reproduces IDENTICALLY (1.881e-04) across the two sets. First harness leaned on --max-len and was refused -- 0110's refusal is the feature working; texts are now fitted by construction | 0.5.0 P8 | done |
| [0138](0138-gte-hub-adoption/TASK.md) | **gte becomes the 7th catalogue model, and the C++ packer mirror lands WHOLE-FILE byte-identical -- T52 closes.** hub: built-in row with the weights pin verified locally first, bfp16 adoption record citing 0137's +0.06/-0.06, `kFilesGte`, probe_repo routing for model_type "new" (tile_n derives 48), pack dispatch. `prepare_model_gte` ports every pack_gte assert (Q-third bias fold exact at 0.125; F16 handled by the existing widening reader); **MSVC powf proven bit-identical to numpy on all 32 rope_inv_freq values by a standalone probe BEFORE writing the packer**, plus a CPython-repr-compatible float formatter -- full-file sha256 equal on BOTH paths (cached blob AND C++-regenerated blob, which reproduces 0133's pinned hash). `list` shows gte ready; `embed gte-multilingual-base` with NO --artifacts runs on the NPU, reports the adopted bfp16 datapath, bit-identical to the explicit form and to 0136's stored output. Gate re-runs: semantic 7/7 models PASS (gte 30/30), tail 7/7 PASS (gte reproduces 0137's baseline to the digit), nomic bit-identity hash unchanged | 0.5.0 P5 | done |
| [0139](0139-t46-memtile-leg/TASK.md) | **T46 closed from data already on disk: the memtile-L1 leg is TWO-THIRDS IDLE while LOCK_STALL binds -- the DRAM leg is the one that matters.** Exact interval-union coverage over 0130's stored trace: input ports 30.9% + 3.7% of the steady-state span, LOCK_STALL 52.8% -- the link sits idle while the core waits for operands, the signature of an upstream bind. The second accounting is ONE ARGUMENT, as the thread predicted: `traffic_bytes(leg="memtile")` -- the legs differ only in B's x4 row broadcast (aggregate 2.0-2.5x DRAM, 14-23 GB/s per mem tile at measured dispatch times). L1-register = 0049's anatomy + 0130's window rate, already owned. **Flag for T48's eventual build**: B-reuse removes B's DRAM re-streams but NOT its L2->L1 broadcasts, and speeds the dispatch 1.72x -- exactly the change that re-binds this leg; the build must re-run this port analysis. T45's shape-dependent-fixed-cost residual narrows to schedule effects and stays a residual | 0.5.0 P7 | done |
| [0137](0137-gte-gates/TASK.md) | **The gte gates: everything PASSES, and the MTEB verdict is the cleanest in the catalogue.** English MTEB (0103's exact protocol, one symmetric invocation, seq 64 both sides, fp32 baseline REPAIRED per 0134/0136's three landmines -- the harness now applies repair_rotary and refuses a non-fp32 load): mean **+0.06 / worst -0.06** vs the -0.5 line, with TwentyNewsgroupsClustering -- the cell the gate is historically decided on -- POSITIVE (+0.34). Multilingual STS as evidence (user decision: non-gating in 0.5.0): STS17 +0.00, STS22.v2 -0.02, all 29 per-language subsets in [-0.30,+0.20], per-subset scores stored as the 0.6.0 gate's baseline. Tail baseline: median 3.4e-04 / p99 5.2e-04 / max-med **2.2x** -- no bge-large tail, 0129's depth-12 expectation measured. Semantic corpus v6: three lang-tagged no/de/es clusters scored only for multilingual models -- a scratch probe showed English models pass them on shared-subword LUCK, so gating them there is meaningless both ways; the six see their pre-v6 corpus bit-for-bit, gte 30/30. Attempt 1 died silently at ~2.4 GB mid-clustering (0103 problem 1's shape); relaunch reproduced all completed cells bit-identically | 0.5.0 P5 | done |
| [0136](0136-gte-runtime/TASK.md) | **arch 3 runs end to end on the NPU -- and the activation key stops being write-only without moving a single nomic bit.** Step 0 first: the dispatch refusal now CALLS `encoder_implemented()` instead of restating its literals (the single-source the comment always claimed). arch-3 branch refuses a container without `rope_inv_freq` (never derives from theta); activation is DATA (silu/gelu-erf, unknown refuses, arch-2 missing-key = silu) -- **nomic bit-identity PASS**, same sha256 before/after; RoPE built from the container's 32 values; tokenizer via an `AnyTokenizer` facade loading `tokenizer.xlmr_table` with the WordPiece path's exact truncation semantics (8/8 byte-exact vs AutoTokenizer in-process). **gte vs oracle on hardware: 1-cos 2.25e-04..3.68e-04** (the bfp16 decade, on `artifacts_nomic_bfp16`); semantic gate 24/24 with control-fails-as-required; goldens built (`make_goldens_gte.py --seq`) and check 17/17 with three discriminating controls >=4 decades worse. **Found a THIRD uninitialised-buffer landmine**: v5 garbages `embeddings.position_ids` too -- SentenceTransformer's derived-position path is silently position-scrambled when the garbage is in-bounds (9.0e-02 -> 6.7e-08 after repair); every future MTEB/ST run of gte must carry `repair_rotary`. Remaining: hub/catalogue+adoption, MTEB, C++ packer mirror | 0.5.0 P5 | done |
| [0135](0135-gte-container/TASK.md) | **arch=3 packed: the gte container, bit-verified against both its sources, on the design set that already ships.** `pack_gte` + `ARCH_GTE_NEW_ROPE_GEGLU=3`; 1.0 GB, 150 tensors, arch-0/2 emission order kept (byte-parity anchor for the C++ mirror). Real biases on qkv/attn_out/ffn_down with the Q-THIRD OF THE BIAS scale-folded alongside the weights ((xW+b)s = x(Ws)+(bs), RoPE linear); `rope_inv_freq` carried as 32 f32 DATA values with a note that deriving from rope_theta alone is wrong by 1.9e-02; ffn_up is the checkpoint's own fused up_gate (lo*act(hi) order); `activation: gelu` becomes a READ field; XLMRTOK1 blob as `tokenizer.xlmr_table`; fail-closed asserts on every 0134-settled config fact; --int8 refuses (no gte calibration oracle). Verification all exact: qkv/ffn_up untile round-trips bit-identical, biases verbatim/scaled as intended, inv_freq bit-equal to the oracle, blob sha equal to 0127's -- and **b_layout_hash 94266693... == the shipping nomic design's**: the plan's central bet closed end to end, zero new exports | 0.5.0 P5 | done |
| [0134](0134-gte-oracle/TASK.md) | **The gte-multilingual-base oracle validates per-layer at 1e-06 -- and the fp32 REFERENCE itself was broken as loaded.** `reference/encoder_gte.py` (numpy) matches the repaired trust_remote_code model at 7e-08..1.9e-06 relfro on all 12 layers, CLS 1.8e-06. **The NTK theta trap defused with a measurement**: the truth is `inv_freq_i = 160000^(-i/32) / 8^(1/32)` -- a scaled base AND a constant correction, NOT expressible as any single theta (the constructor's second cache build takes the NTK branch because `self.max_position_embeddings` is never updated); verified bit-for-bit (max|diff| 0.0) against a freshly constructed module. Negative controls: plain-20000 misses by 1.9e-02, 160000-no-correction by 8.9e-03 -- both in the looks-fine-is-wrong regime 0068 warned about. **Two reference landmines recorded**: transformers 5.15 materialises the remote code's persistent=False rotary buffers as UNINITIALISED memory (run-to-run garbage; repaired by fresh construction), and `torch_dtype: float16` in the config is honoured silently (~1e-03/layer until loaded fp32). Both hit any MTEB/ST use of gte on this env. Container consequence: bake the 32 inv_freq values, not a rope_theta field | 0.5.0 P5 | done |
| [0127](0127-t52-unigram-generator/TASK.md) | **T52 step 1: the SentencePiece Unigram generator + reference encoder, byte-exact against HuggingFace 343/343 across 19 categories** (CJK no-space, NFD-decomposed jamo, NFKC, emoji/ZWJ, unk-fusion, whitespace edges). `gen_xlmr_tokenizer_table.py` (stdlib, fail-closed like the gemma precedent) emits XLMRTOK1 (5.3 MB: charsmap Darts trie + strings blob verbatim, f64 scores, Metaspace/template metadata); `xlmr_tokenizer_ref.py` consumes the BLOB, not tokenizer.json -- the executable spec the C++ port diffs against. Three load-bearing findings: **scores are f64** (65,856 of 250,002 log-probs do not survive f32 and HF sums f64 in Viterbi -- trusted the data over the brief); **HF's Precompiled normalizer deviates from sentencepiece** (graphemes <6 bytes replaced whole by first prefix match, confirmed live on U+1E9B U+0323); fuse_unk on, `str.isspace()` != Rust White_Space. Deliberate recorded gap: no added-tokens splitter, deferred to the C++ task. gte tokenizer files pinned by sha256 | 0.5.0 P4 | done |
| [0131](0131-t48-gate-decision/TASK.md) | **T48 gate 2: B-reuse re-priced on MEASURED numbers -- 1.715x array / 1.310x e2e on bge-large -- and the build is NOT funded.** Conservative arithmetic: subtract only the saved B bytes (31/32 of 32 row-block re-streams) at 0130's 45.5 GB/s roof from 0128's measured 8-col dispatch times; the register's own "expect below 1.80x" prediction was right. Gate (a) passes (1.72 >= 1.5); gate (b) FAILS on 0126's census -- no spare mem-tile input exists, so a build means the cascade C-collapse (0047: dataflow proven, kernel scalar, vectorisation unfunded -- "not a session") or a hierarchical re-join. T48 stays OPEN as a priced design task with a named trigger: fund the vectorised cascade microkernel, first target bge-large at k=64 (container format unchanged), prize 1.72x/1.31x | 0.5.0 P3 | done |
| [0130](0130-t45-traced-roof/TASK.md) | **T45 closed: the roof is a MEASUREMENT -- 45.5 GB/s on the hardware timebase -- and the wall clock was telling the truth.** One traced bfp16 dispatch at production tile (64,64,48), 4 cols (trap 7), bge-base ffn_up at M=8192: kernel window 1658 cyc at 118.5 MACs/cyc (46.3% of MMAC peak), kernel-start pitch 2932 cyc -> duty 56.6%, effective 67.0 MACs/cyc/core. 6144 invocations x pitch = **9.97 ms on the 1.808 GHz core clock vs 10.006 ms by 50-iter wall clock -- 0.4% apart**, resolving T45's y-axis doubt by cross-instrument agreement. 453 MB -> **45.5 GB/s**; four instruments now agree on the roof (~44 inferred, 43.8-46.7 probes, 44.0-45.5 M-sweep marginal, 45.5 trace). LOCK_STALL covers ~53% of the traced span: the bfp16 datapath is traffic-bound ON the array, measured. Residual to T46: the fixed cost's shape-dependence (180 vs 278 us on one geometry class) | 0.5.0 P2 | done |
| [0129](0129-t51-int8-tail/TASK.md) | **T51 step 1: the tail is the INPUT's, not the tile's.** 0122's 224 texts through the shipping int8 bge-large at tile_n=64 (no build) -- a different tile geometry AND an arithmetically unrelated quantisation scheme -- produce the **same worst words** as bfp16 at tile_n=32: newsletter/contact/sermons/cinema/messages, log-Pearson **0.834**, top-10 overlap 8/10. `tile_n` exonerated; the number format exonerated as the CAUSE -- the tail is a property of (model x input): bge-large's forward pass amplifies ANY low-precision perturbation on single-word inputs. The bfp16 control re-ran in-session and reproduces 0122 to the digit (median 2.18e-04, max 2.18e-02, same five violators). int8's absolute level is worse (median 2.21e-03, max **1.08e-01** on `newsletter`; 61.6% over the gate -- consistent with 0085's known int8 bge-large 1-cos FAIL). Consequence for the step-3 gate: **per-model, per-datapath, on a p99** -- a max-gate is uninformative where the median already fails, and a mean hides a 30x p99/median. Step 2 re-scoped: profile depth, not tiling | 0.5.0 P6 | done |
| [0128](0128-t45-m-sweep/TASK.md) | **T45 item 2: the M-sweep intercept -- the ~44 GB/s roof SURVIVES separating the fixed cost, and the fixed cost is not one number.** `--probe-streams` extended to all four batch tiers (a one-line filter removal) = a 16-point sweep spanning ~0.7-604 MB per design set, with 0124's corrected byte accounting. Least-squares t = t0 + MB/B: base_bfp16 **180 us / 44.0 GB/s** (R2 0.979), nomic_bfp16 **278 us / 45.5 GB/s** (R2 0.992) -- the marginal rate on hidden-768 bfp16 is the roofline's inferred roof, now with an intercept. But nomic and base share a geometry class and differ 278 vs 180 us, which a shape-independent per-dispatch constant cannot explain -- **the two-parameter model is incomplete**, and the residual structure is exactly what T45 item 1 (the traced dispatch) must disambiguate. Narrow shapes read lower marginal rates (MiniLM 36.8, its int8 twin 29.6). bge-large's set carries only the top tier -- no sweep without a re-export, recorded. Prior fits 150/573/627 us: this lands near 0010's, not 0048/0080's | 0.5.0 P2 | done |
| [0126](0126-t48-channel-recensus/TASK.md) | **T48 gate 1 done, and its conjecture refuted: the channel re-census on the SHIPPED sets is bit-for-bit 0046's.** Cache dirs matched to shipped xclbins by 0-byte content diff (trap 7c -- never mtime), covering all three geometry classes (48x8 bfp16, 32x8 bfp16, 48x8 bf16). Every core tile 2/2 in, five of eight mem tiles 6/6 in, C join spending 4 of 6 -- identical across classes, because **a DMA channel is an integer allocation: narrowing C to bf16 changed bytes per transfer, not channel count**. So T48's "expressible wiring" gate FAILS on the current topology: the cheap version of B-reuse (a fifth fifo) does not exist; a build means re-plumbing the C join (0047's cascade collapse, vectorised kernel unfunded) or a hierarchical re-join. Spare inputs only on (0,1)/(1,1) (one each) and (7,1) (two) -- three tiles cannot broadcast to eight columns through five full ones. Gate 2 (re-price on T45's measured roof) remains | 0.5.0 P2 | done |
| [0125](0125-t47-gbs-audit/TASK.md) | **The GB/s audit closing T47: nine shipped design sets re-probed with the fixed accounting, on a quiet machine (CPU 4%->1%, all resident contexts Idle).** The docs needed less correction than feared -- **no current doc quotes a probe-streams GB/s**; the inflated figures live in task logs (kept, rule 3b) and T47's own thread text, and `tools/roofline.py`'s own accounting was already correct (0120 found the bug by diffing the two). The fixed binary reproduces 0097 t21's hand-corrected MiniLM int8 numbers to ~2% (35.5/22.1/36.3/31.6 vs 35.9/22.8/36.8/31.7) -- the correction now agrees from two directions. The bfp16 large shapes cluster at **43.8-46.7 GB/s**, an independent per-dispatch corroboration of 0120's inferred ~44 GB/s roof (still host-observed -- T45 owns the y-axis); `attn_out` reads low on every model (T45's fixed-cost fingerprint, visible per dispatch); int8 sits lower than bfp16 on identical shapes, consistent with the same fixed cost on shorter dispatches | 0.5.0 P1 | done |
| [0124](0124-t47-t50-runtime-fixes/TASK.md) | **One rebuild, two refusals: T47's byte accounting fixed and T50's two fail-opens closed.** `DesignInfo` gains `tile_n`/`cols` read from design.json; `--probe-streams` uses `a_elem_bytes` and REFUSES a pre-field design rather than substituting 48x8 -- verified by MB-column hand-checks on int8 (ab=1), tile_n=64 (bge-large int8n64) and a bfp16 control. `--cpu` on the BERT family now refuses on BOTH CLI forms (the subcommand rewrite forwarded the flag it used to drop) since there is no host BERT encoder to route it to; arch-1 still honours it -- though its control needs a host-only container (`GemmaEncoder` reads raw q_proj F32; fails loudly on the pretiled one, pre-existing, recorded). The flag form's no-`--artifacts` default no longer means the stale width-384 per-op set: resolution defers until the container loads and goes through the same `pick_artifacts()` the subcommands use, catalogue datapath included -- bge-base's legacy form went from a misleading staged-buffer error to running on `artifacts_base_bfp16`. Semantic gate 6/6 on the rebuilt binary | 0.5.0 P1 | done |
| [0123](0123-register-hygiene/TASK.md) | **T49 closed, T52 filed, T44 re-ranked — Phase 0 of the 0.5.0 plan, no rebuild, no hardware.** `check_register.py` gains check 2b: a closed thread carrying a bold REOPENED annotation with neither a section back in OPEN-THREADS nor a later `SUPERSEDED by [Tnn]` block FAILS the run — **proven on a planted copy** with T2's superseding block removed (fires exactly as it would have on 2026-08-22, the day T2's annotation was written and ignored). The check's trigger is the bold *marker*, not the word, because the first draft of T49's own closure text would otherwise have failed the check that closes it. T49 check 2 resolved **editorially**: a design.json grep would structurally miss decision-premises (T27's was a decision until 0104 made it a config field), so the fix is a preamble rule — **a closure names the condition it depends on**. Filed **T52** (SentencePiece Unigram — the third tokenizer family, gating gte-multilingual-base and the XLM-R/mE5/mGTE line; 0055's unspent ~600-900 LOC estimate, 0061/T29's read-the-tokenizer.json-first lesson). **T44 re-ranked**: gte-multilingual-base's four GEMM shapes are a literal match to `artifacts_nomic_bfp16/gemm_rtp/design.json` incl. `b_layout_hash` — verified in-tree, zero new exports; its blocker is T52, not T43. Fixed CLAUDE.md's own premise decay: rule 3 said "1 thread, 163 lines" while line 583 said ten | 0.5.0 P0 | done |
| [0122](0122-bge-large-short-input-tail/TASK.md) | **bge-large has a 100x accuracy tail on single-word inputs, and every gate we own averages it away.** Started from the first reference vector this project has ever checked with **no shared code lineage with us at all** -- a user obtained embeddings for the word "test" from an online service. bge-small matched at the 3-decimal quoting floor (1-cos 2.38e-05, mean |delta| 0.00029 against a 0.00025 floor); bge-large did not, and re-measuring against full precision confirmed **the external reference was right and we are off** (2.234e-03 against our own 2e-03 gate). Measured over 224 varied texts: median **2.18e-04**, max **2.18e-02**, p99 6.63e-03, **5 over tolerance -- every one a single word** (`newsletter` 2.18e-02, `sermons`, `contact`, `cinema`, `messages`; "test" is only sixth). **Sentences and phrases stay within 1.24x of the median**, and bge-base on the IDENTICAL bfp16 datapath has max/median 3.6x and zero violations. Deterministic: bit-stable across batch compositions and reproduced to four significant figures through a second reference implementation. **Two mechanisms tested and REFUTED** -- cancellation (||CLS|| is 16.5-19.1 for every word and "test" has the LARGEST; corr +0.111 where the hypothesis needs -1) and bfp16 block-floating-point outliers (crest factor over aligned blocks of 8 is 1.879-1.909 across every word, max|h| 17.6-18.0, corr -0.00/-0.20/-0.28). **No input statistic predicts the error**, which is what redirected the work from cause to distribution. A third probe was VOID and is kept: `normalize_embeddings=False` still returned unit vectors because bge-large's ST config carries a Normalize MODULE, so the flag governs only the final step. **The finding with the longest reach is the methodology**: 1-cos (2.626e-04), MTEB (+0.13/-0.01) and the 0121 semantic gate (24/24) all PASS bge-large, because all three report a central tendency and none looks at a tail. CURRENT_STATUS's 1-cos column now says so. Filed as **T51** with three priced next steps | measurement | done |
| [0121](0121-semantic-gate/TASK.md) | **A semantic gate: do the vectors MEAN anything? All six models, ordering only, no tolerance.** Every existing gate is RELATIVE (NPU vs sentence-transformers) and shares one blind spot -- it passes when both sides are wrong the same way. This one needs no reference: 12 clusters x 3 sentences, and for each of the 24 lexical-paraphrase sentences its own cluster partner must be the NEAREST of the other 23. **All six pass, margins +0.189 to +0.387.** STDLIB ONLY (630 dot products in pure Python), so it runs against a **cold dist zip** with nothing installed -- the one situation where meaningless vectors are most likely and least likely to be noticed. **The valuable half is sec 3: FOUR corpus revisions, every failure a defect in the CORPUS rather than in a model** -- "boxes"/"box" at +0.456; cooking and gardening both growing vegetables; weather and astronomy both about the night sky; and finally the twelve semantic paraphrases forming an accidental 13th cluster of first-person errand stories. v5 fixed the sentences and was STILL not enough, so the **test design** changed instead: the semantic tier became a reported PROBE, never a veto, because what it tests is a checkpoint's world knowledge (MTEB's job) and a gate firing on "is a hedge related to a rose" gets switched off. The negative control runs every invocation and **found a flaw in itself** -- rotate-by-one is nearly the identity on a cluster-major corpus and nomic PASSED it; rotating within the gate subset provably breaks all 24, measured 24/24 on every model. Global separation is reported and never gated (a max over 264 far pairs is an extreme-value statistic -- verify_embed_e2e's own argument about testing corpus size). Two runtime CLI defects found and filed as **T50**: `--cpu` accepted and IGNORED by `embed` (removed from this tool rather than shipped), and the legacy `--model X --embed` form picking no design set (fails loudly above hidden 384). The gate also scrapes `datapath_reported` from the runtime's own status line, so it certifies WHICH datapath produced the vectors -- bge-small alone reads plain bf16. New `tools/verify_semantics.py` + `tools/semantic_corpus.json` | testing | done |
| [0120](0120-roofline-analytic/TASK.md) | **Roofline, step A: all 24 shipped GEMM dispatches placed from geometry alone, no hardware.** The chip needs THREE compute ceilings, not one — plain bf16 rides the fp32 vector unit at 32 MAC/cyc/core while the MMAC idles (0049), bfp16 emulation reaches 256, int8 512 — so `emulate_bfp16` moves a dispatch to a *different roofline*, not along one. x is pure arithmetic over `design.json` and immune to rule 1; y is where rule 1 bites, so measured points are a separate series carrying 0109's own "host-observed, not a hardware trace" caveat. **Reproduces all three known diagnoses**: 0048's discriminating pair lands 1.50x apart on x and 1.1% apart on y (flat roof); the same pair under int8 separates in the byte direction (0080's traffic-bound regime); and the four shipped bfp16 models imply **41.3-46.8 GB/s**, a 13.3% spread against GMAC/s's 28.2%, i.e. a bandwidth roof at ~44 GB/s. **Headline: bfp16 adoption raised the ceiling 8x and bought ~2x, because production runs 5-7x LEFT of its own ridge point (AI ~120 vs 741).** Prices B-reuse — now the largest traffic term at 46-52% on every model — at **1.80-2.02x array / 1.18-1.34x end to end** on the datapath that ships, against 0048 retiring it on a datapath only bge-small still uses; still channel-blocked (0046). **Refuses** `tile_n` 48 and 64 on bge-large, by different constraints. Two bugs found: `--probe-streams` hardcodes A/B element size 2 and `tile_n*cols` 384, inflating every published int8 GB/s by **1.57-1.85x** and differentially; and **T27 is closed on a premise 0103/0104 reversed** ("bfp16 was never adopted") while T2 sits in CLOSED-THREADS annotated REOPENED. Filed **T45-T49** (the register went 3 -> 8 live threads); T2 and T27 gained pointers forward rather than edits. New `tools/roofline.py` | measurement | done |
| [0119](0119-release-sweep-040/TASK.md) | **Cutting 0.4.0: the documents, and 130 broken links nobody had noticed.** Scope set deliberately -- accuracy/throughput/interleaved re-run, MTEB and energy carried forward, because 0118 is bit-identical so a six-model MTEB pass ("hours") would reproduce the same numbers. **The sweep REFUSED: the machine was not idle**, and reading the harness confirmed the guard covers every timing stage, so there is no partial run that dodges it and `-AllowCpuContention` is exactly wrong for a release table. README had four claims true of 0.3.0 and not of this build -- a performance table with no gemma row, gemma still called "host-only", "we have not exported or measured" a long-sequence design when 0112/0113 exported two, and a Contributing section pointing at three closed threads. Then `sync_public_repo.py`, run as the gate it is: **one hard FAIL** (a link to excluded material that survived the rewriter only because it was ALSO relative-wrong) plus **68 in `docs/history.md` alone** -- 0075 moved 1,027 lines out of `CLAUDE.md`, which sits at the repo root, so every relative link in them broke on arrival and had been broken since. **The transferable failure is my own first pass**: seven links named a task by NUMBER with a stale slug, a rule redirected them on the number alone, and that is a guess about whether the author meant the number or the topic. Reverted whole; each of the seven then checked against the CONTENT of its target (0032 §2 really is "eltwise ops must EARN the dispatch"). The number was right every time -- but that is a finding now, not an assumption. 15 links left broken on purpose: `tasks/README.md` rows quoted verbatim into the log that added them, correct for their real home. **And the sweep caught a regression 0118 had introduced**: gemma's accuracy stage ran the runtime with no `--prefix`, which 0118 made a refusal -- and 0074's stored control vectors were made under the old silent `"document"`, so the prefix is part of the control, not just a missing flag. Fixed and pinned. Clean sweep then: **every accuracy figure reproduces 0105/0108 to the digit**, and across two runs twenty minutes apart NPU throughput repeated to **0.2%** while the CPU baselines moved 3-4% -- the README's +-20% ratio caveat, measured instead of asserted. Zip built and cold-tested; the container hash check FAILED and the failure was informative: tensor data byte-identical, the whole difference 17 bytes of JSON (`a_dtype`, added by 0104 after the repo's container was packed), so a whole-file hash cannot tell a mispack from a format field | release | done |
| [0118](0118-prompt-name-per-request/TASK.md) | **The task prompt moves from a process flag to a request field, and loses its default.** `--prefix` was resolved once per `serve`, so one server could answer only one kind of query -- a RAG deployment needing `search_query` *and* `search_document` had to run two, each holding an `hw_context` on a shared NPU. Now `"prompt_name"` in the POST body, **required** for a model with a prompts table, **400 listing the valid names** when missing/unknown/not-a-string, and `GET /health` advertises `prompt_names` so a client never has to provoke the 400 to discover them. The field name was chosen against a survey, not a guess: OpenAI has no such field (its models are symmetric), Cohere **requires** `input_type` and nomic's four names are Cohere's verbatim, vLLM puts `input_type` on its OpenAI route, **TEI deliberately kept `prompt_name` off** its OpenAI route -- so `prompt_name`, because EmbeddingGemma's table holds 14 *sentence-transformers* keys (`STS`, `BitextMining`) that are not "input types". Also removed **the last silent default in the runtime**: Gemma's `--prefix` defaulted to `"document"`, one of fourteen, on a checkpoint naming none. **Bit-identical on all three model families** for the same effective prompt, and endpoint-vs-`--embed` is **0.000e+00** on each -- so the CLI and HTTP prompt paths provably agree. `verify_endpoint.py` had **zero** prefix coverage and now proves the feature rather than only its refusals (two prompts, one session, vectors differ). Found a **pre-existing** break while doing it: `compare_three.py` has passed `--prefix ""` to BERT models since 0071, which is a refusal, not a no-op | M13 | done |
| [0117](0117-t3-t28-repricing-retire/TASK.md) | **T3/T28 retired on a measurement, not an opinion.** The register asked for the relay to be re-priced against the post-fusion breakdown; this is that. Single-lane, both ends of the catalogue, array bracketed idle (bge-large's **56.6%** array share reproduces 0109's 56.7% in a separate session). After 0108 the host GELU is no longer a bucket -- it lives inside the fused read-out pass -- so the relay's target is only the `ffn_up`->`ffn_down` share of transport: **13.8% of wall on MiniLM, 8.7% on bge-large**, i.e. best case **1.160× / 1.095×** against 0107's 1.25-1.35×. **And the verdict does not rest on the 44.4% N-split**: credit the relay with the ENTIRE transport bucket and the bound is 1.450× / **1.242×** -- on bge-large barely above the **1.223× 0108 already delivered** for a C++ rewrite with no array work, with array time rising, a hand-written slow relay matmul, and possible design switches all priced at zero. Mechanism proven (0054, 0092), value collected cheaper -- the T2/T9/T12 precedent. T3's 33% figure is three datapath generations old and retires with it; reopening trigger stated (host-dominant work, which at seq 512 is attention, i.e. T42) | research | done |
| [0116](0116-t41-golden-seq-flag/TASK.md) | **T41 closed: the golden pipeline takes `--seq`.** All three makers, defaulting to their corpus module's constant, so nothing that omits the flag changes. Every `SEQ_LEN` use was verified to be inside `main()` first, so one shadowing assignment after `parse_args()` reaches all of them. Verified two ways: the default reproduces nomic's committed s64 golden **bit-for-bit**, and `--seq 256` reproduces **byte-for-byte** the file 0112 made by editing the constant -- the flag and the hand edit are demonstrably the same operation. **One thing looked like a regression and was not**: MiniLM's golden came out with a different hash, and it took three checks to settle -- deterministic (two regens agreed), not this change (the pre-change script from `git show HEAD:` gave the same new hash), and **not the data** (all 14 tensors bit-identical, the sole difference a `'pooling': 'mean'` metadata key added after that file was committed). **A golden's hash is not its contract** | research | done |
| [0115](0115-t34-serve-arch1/TASK.md) | **T34 closed: arch=1 gets the HTTP endpoint, and it is the SAME endpoint.** arch=1 never lacked a `--serve` path because EmbeddingGemma is incompatible with one -- it lacked one because the endpoint was **written against the BERT encoder's type**, 266 lines inline reading `EmbedService`. The real coupling was three members wide (vocab size, prefix text, "embed these texts"), so `serve_http()` now takes exactly those and **both architectures run one server** -- no second implementation to drift on the OpenAI shape, the four 400 cases, base64, or `InputTooLong` -> 400. Gated on agreeing with arch=1's own `--embed`: **base64 bit-identical, 0.000e+00** (the float arm's 5.215e-08 is the `%.7g` JSON text form, not an encode difference -- my first gate demanded == 0.0 and failed a working endpoint). The `--cpu` path serves too, because `serve_port` is parsed before that branch and leaving it unhandled would have made `--serve --cpu` embed a placeholder once and exit -- a silent no-op; its id is `<model>-cpu`, never `-npu`. Item 3 re-measured and two of its levers turned out already spent: C readback **11.5% -> 2.2%** (`--c-bf16` shipped) and **GeGLU 4.5% -> 0 ms** (0108's port), leaving host attention (**20.6%**, now the largest host bucket) and the RMSNorm chain (12.7%). Regressions bit-exact: MiniLM **2.430e-02/3.406e-04** and gemma **1.749e-04/2.315e-04** against 0104/0105. `make_release.ps1` now ships `artifacts_gemma_bfp16`, whose exclusion comment named the serve gap as its only reason | M13 (T34) | done |
| [0114](0114-t38-pad-dimensions-probe/TASK.md) | **T38 answered: mem-tile padding routes and works at attention's geometry, on all 8 columns.** The build T38 asked for instead of an argument. Three stages, every check **bit-exact**: pad 8->16 on the mem tile (`cols=1` and `cols=8`, padded half exactly 0), un-pad as an ordinary strided mem-tile read, and a compute tile consuming a padded stream. **So 0043's `cols <= 4` is a property of a design that declines to pad, not of the hardware.** note 0007's prediction that "the output side is where a first attempt will fail" was collected exactly: the first form put pad and un-pad on one hop and returned **MISMATCH 2.121e+00**, because a shim DMA consumes the stream in order and cannot skip -- a drain tap says where bytes LAND, not which to keep. Also hit **trap 7d live** (a variant served another's binary; `cols` was a closure variable, which `iron.jit`'s cache key never inspects) -- the seventh instance of the stale-binary class and the **first that failed loudly**, only because the argument sizes disagreed. Value question answered too and its **sign flips with seq**: at 64 the array is the bottleneck so folding attention is the wrong direction (0043 was right); above seq ~470 it is not. Successor T42 | research | done |
| [0113](0113-t40-seq512-close/TASK.md) | **T40 closed: host attention overtakes the array at seq ≈ 470.** nomic repacked (`--max-seq 512`, its own container name so nothing shipped is touched) and re-exported at `--seq 512 --batch 16`. Across an **8× sequence range the array does not move — −2.0%** on identical M and identical 192 dispatches. Attention goes **2.49× for 4× (64→256, sublinear) then 2.04× for 2× (256→512, textbook linear)** — the small-seq discount is spent by 256, which **corrects 0112's own extrapolation (projected ~2100 ms, measured 1720.6, 22% high)**. Per token seq 512 costs **1.605×**, and attention alone is **52.1% of wall clock** against the array's 46.6%. **F3's premise — attention at 2–5% — is a seq-64 number.** Item 2 answered too and it is about SMALL requests: `use_tier()` rounds up, so the ladder's span is irrelevant and the smallest tier's token footprint is what bites (2048 slots vs 256) — measured at **5.8× for a single text**, 11.5× at 64. Two per-token buckets *fall* with seq (`bias` 540→404 ms, `conv` 32→22), the only argument found in favour of long sequences. Contention gap stated, not smoothed: two of three runs sat in a window with 14 foreign submissions, so a **third bracketed run** (zero growth) was taken and reproduces them to 0.6% | research | done |
| [0112](0112-t40-seq256-nomic/TASK.md) | **T40 measured at seq 256, and the array does not move.** nomic re-exported at `--seq 256 --batch 32` (same M = 8192, so both tiers were JIT cache hits — `M` is the key, `seq` is not). Two runs per arm, identical **163,840 tokens** each: **NPU dispatch+wait 1554.5 vs 1568.8 ms, −0.9%** — 0110's read-derived "seq is compiled into nothing" confirmed on hardware. **Per token seq 256 costs 1.221×, and all of it is host-side**: attention **2.487×** (against 4× for a pure O(seq²) term — longer rows amortise the AVX2 loop), softmax-over-scores 1.31×, while the per-token buckets stay flat (`bias` 0.90×, `conv` 1.03×). **The balance inverts**: array share of wall 76.3% → 62.0%, attention 16.5% → **33.6%**, and `attn` overtakes `bias` as the largest host bucket. **F3's conclusion may survive; its premise does not** — 2–5% is a seq-64 number. Two corrections to T40's own filing: the 256 cap is a **config field** (`max_seq_len`), not the position table, so **nomic needs a repack above 256 too**; and **the validation goldens are seq-shaped** — `--bench` refuses without them, and regenerating them needs an edit to `reference/corpus_nomic.py`'s `SEQ_LEN = 64`, the same hardcoded-seq class 0110 removed one layer further in. Fixtures backed up and restored, `git diff` empty | research | done |
| [0111](0111-t13-pretiled-stability-at-scale/TASK.md) | **T13 answered on hardware, and the instrument that raised it was the wrong one.** The pre-tiled instability is **real at production `M`** and does not dilute — spread **15.0%** at M=8192 against row-major's **0.1%** in the same session, with M=512 first reproducing 0007's 140.9 MACs/cyc to the digit. But the per-invocation trace shows the two arms are the **same kernel up to p90** (median 1450 vs 1422 cyc): **13 of 365 invocations (3.6%)** run 2,175–19,192 cycles and carry **19.7% of all cycles**; strip that tail and pre-tiled is **139.8 vs 140.7 MACs/cyc — 0.6% apart**. Not the permutation (all four `order × inner` variants carry it), not B's L1 depth (`--b-depth 3`, the deepest the 63 KB budget allows, changes nothing). **And untraced it WINS**: 50 iterations, quiesced NPU, pre-tiled **3348 µs / 2884.7 best** against row-major's 3568 / 3260.9 — **1.066× mean, 1.130× best** on the shape whose per-core cycles call it 12% slower. At 4 columns the dispatch is not compute-bound, and per-core MACs/cyc measures the compute window *including the core's waiting* — CLAUDE.md F2 met head-on. Also corrected the experiment as 0093 specified it: **"8 columns, traced" is not expressible** (trap 7; `TRACE_ROUTING = {2, 4}`), so the column axis stays unmeasurable and the untraced win is the only evidence for production's width | research | done |
| [0110](0110-refuse-silent-truncation/TASK.md) | **An input that does not fit is now an ERROR, not a shorter input — and `seq` becomes an export flag.** Both tokenizers cut at `max_len` and said nothing, and `n_tokens` was **capped by construction**, so a truncated text was indistinguishable from one that fitted right through to `usage.prompt_tokens`. The fail-open class of traps 6b/6c/7c/7d with the worst blast radius yet, because what is wrong is the **answer**: the vector is correctly shaped, correctly normed and deterministic. **Demonstrated, not argued** — two real Norwegian saksframlegg sharing an administrative preamble truncate to **byte-identical** token streams. The runtime now refuses on all four encode paths, naming the input's index and its real token count; `serve` answers **HTTP 400 `invalid_request_error`**; `--allow-truncation` restores the old behaviour and warns once per run; `--tokenize` deliberately still truncates, because it exists to diff against HuggingFace, which truncates too. Second half: `SEQ = 64` was a module constant with no flag, now `tools/export_gemm_rtp.py --seq` — cheap because **seq is compiled into nothing** (it enters only as `M = batch * seq`, and the runtime inverts the split at load). Golden gate re-run, PASSES on arch 0, 1 and 2. **No long-sequence design has been exported or measured** — filed as [T40](../research/OPEN-THREADS.md#t40) | M13 | done |
| [0109](0109-fused-ratio-energy/TASK.md) | **The two columns 0108 invalidated, re-measured — and the balance has inverted.** NPU/CPU **1.885 / 1.688 / 2.734 / 2.691 / 3.553 / 1.392x** and energy **3.10 / 2.73 / 4.17 / 4.79 / 4.01 / 3.72x** better than CPU, interleaved per 0040, `\Energy Meter` per 0034, NPU idle throughout. **Array time is unchanged by the fusion**: `wait (hardware)` moved −0.1% to +2.2%, turning a claim CURRENT_STATUS had explicitly labelled an *argument* into a measurement. But the array's **share** rose on every model (bge-large 46.4% → 56.7%) and the array-infinite ceiling with it (1.87 → **2.31x**) — the host lever is largely spent and **the array is again the larger remaining piece**, the opposite of what the docs have said since 0081. Also found a real tool gap, flagged not fixed: `--guard-contention` misreads a **genuinely idle** array (zero hw_context rows) as an unparseable format change, and fails closed | M13 | done |
| [0108](0108-fuse-epilogue-bfp16/TASK.md) | **0082's host-side epilogue fusion ported to the bf16/bfp16 datapath — the one five of six models now run.** 0082's fusion was gated on `a_elem_bytes == 1`, so it never fired outside int8. **Measured before building**, as briefed: the bf16 FFN chain turns out to have the *same three-pass structure* as int8's minus the quantisation step, addressable share **17.7–28.3%** of wall clock — not small, so it was worth building. Extended 0082's own `FusedNext`/`gemm()` mechanism rather than adding a parallel one; arch=1 ported too, leaving Gemma's four-norm RMSNorm chain unfused exactly as 0082 did. **Bit-identical**, verified two ways and re-verified independently: the embedding vectors hash the same fused vs `--no-fuse-ffn`, and every model reproduces 0105's `1-cos` to the digit. A/B, three runs per arm, idle machine: MiniLM **1.265×**, bge-small 1.208×, bge-base 1.251×, bge-large 1.223×, **nomic 1.340×**, gemma 1.153×. That lands on 0107's *ceiling* estimate for T28's hardware relay (1.25–1.35×) via a pure C++ rewrite with none of the relay's open risks — which strengthens 0107's park recommendation rather than weakening it | M13 | done |
| [0107](0107-t3-t28-pricing/TASK.md) | **T3/T28 priced before anyone builds the next piece — and the answer is park.** The relay is correct at production width and has no trace, so it was priced from the traffic model instead. On the datapath that **actually ships** (bfp16 + bf16-C, per 0104): addressable share **19.7–25.9%** of the encode, best case **1.25×–1.35×** assuming zero rebuild overhead. That is **smaller than the 1.394–1.482× 0082 already measured on hardware** for a plain C++ host-side fusion — reproduced at three runs per arm in 0084 — which is built for int8 only and **not wired for the bfp16 datapath five of six models now run**. Cheaper lever, better measured, unbuilt where it matters. Also: T3's 33% figure is dated to the pre-adoption datapath, and 0081's 30.4%/1.44× ceiling is an **int8** bound and the wrong comparison — the bfp16 equivalent is *larger*. **And it caught an error in T28's own entry, written the day before by the coordinator**: (b′)'s "needs a third hierarchy tier" contradicts 0083's same-day finding that 8 columns fit in **two** tiers (4+4 or 5+3); the column count had been conflated with the K-reduction depth | research | done |
| [0105](0105-release-sweep-adopted-datapaths/TASK.md) | **The 0.4.0 sweep re-run on the datapaths that actually ship.** 0104 moved five of six models to bfp16; the release harness still pointed at the **pre-adoption** directories and labelled every row `bf16`, so a sweep run as-is would have measured the datapath we had just stopped shipping and called it current. Harness fixed first, and a new scrape takes the datapath from **the runtime's own status line** rather than restating the table's intention — every row's reported datapath matched its intended one. Gains over 0085: MiniLM **+8.3%**, bge-base **+12.6%**, bge-large **+20.5%**, nomic **+9.9%**, gemma **+7.3%**; bge-large's array time 9,734 us corroborates 0104's separately-measured 9,776 to **0.4%**. Found and fixed a real harness bug mid-sweep: the per-stage JSON merge is per-key and every stage pre-declared `datapath_reported = $null`, so the interleaved stage **silently wiped the accuracy stage's real reading** for all six models. CPU was contended throughout (IDE + browser), flagged — but the confound runs *against* the result, and **bge-small is the table's own control**: the one model whose datapath did not change is the one row that did not improve (−2.2%). int8 columns carried over from 0085, **not** re-measured, and said so | M13 | done |
| [0104](0104-adopt-bfp16-per-model/TASK.md) | **bfp16 adopted per model, behind a guard built first because adoption creates the hole it closes.** A bfp16 design and a plain-bf16 one at the same geometry carry the **same `b_layout_hash`** (emulation changes MMAC precision, not B's tiling), and `design.json` recorded nothing about the datapath — so `design_fits()` would have let **bge-small**, which shares MiniLM's hidden-384 geometry and FAILED the bfp16 gate at −0.5010, be handed the bfp16 design by **alphabetical tie-break**. Now: `export_gemm_rtp.py` writes `emulate_bfp16`, `design_fits()` takes a `want_datapath` that refuses a mismatch, the catalogue records each model's adopted datapath, and the status line reads it **off the loaded design**. Proved against a known-bad artifact — bge-small *refuses* (`no NPU design for hidden 384 ... datapath bf16`) against the very directory MiniLM runs happily. Five models rebuilt on `--emulate-bfp16 --c-bf16`, all six re-gated, **nothing that had a prior number moved**. Coordinator review then fixed three things: the catalogue opt-in was **inverted** (a future built-in would silently inherit bfp16 — now an explicit allowlist), **absent ≠ false** for a pre-0104 design (now reports `UNRECORDED` rather than claiming bf16), and the tie-break is now **evidential** rather than alphabetical, preferring a set that records what it is | M13 (T23) | done |
| [0102](0102-toolchain-provenance-audit/TASK.md) | **Read-only audit of every published claim against the mlir-aie 1.3.4 -> 1.4.2 boundary** (`42da31d`) that T26 exposed: ~20 claims, classified by whether the toolchain change could plausibly move them (host-side and file-format claims cannot; rounding-mode-dependent AIE codegen can). **Most are cleared by evidence already on record** - 0058 re-verified SAXPY, the M2 GEMM headline and **CLAUDE.md trap 2b's whole rounding table bit-exact on 1.4.2**, including the softmax row-sum mechanism (min 0.994581 / max exactly 1.000000), and 0060 independently rebuilt a real production GEMM shape post-migration and reproduced `1-cos` 1.086e-05. **Exactly two claims are genuinely expired** - T26's 6.6x anomaly and `--emulate-bfp16`'s 2026-08-18 retirement at 3.470e-03 - and they share one mechanism, both re-measured by 0101. The sharpened rule: risk lies where the rounding-sensitive op happens **inside an `aie_api` header via an ambient intrinsic**, not merely where a measurement is old - our own kernels set the mode adjacent to the ops and were unaffected. The four original BERT models still ship on **1.3.4-built xclbins**, never rebuilt. Wrote `research/notes/0009-toolchain-provenance.md`; priced a `toolchain.json` provenance sidecar at under an hour, filed as **T39** | research | done |
| [0100](0100-docs-catch-up/TASK.md) | **Docs caught up to the thread sweep, and two CLAUDE.md claims corrected against their own sources.** The int8 headline said **1.10x** and blamed the quantisation pass for costing more than the bf16 conversion it replaces - but 0079 itself *measured and refuted* that (quantise is **1.04x**, not more; the real cost is the **C drain**, since int32 C is still 4 bytes so int8 moves no fewer output bytes despite 7x faster MACs), and 0085's sweep supersedes 1.10x with **1.71x-2.39x**. The geometry table said bge-large forces `tile_n` 32; that is now stated as dtype-dependent - 32 at bf16 (64 would need 65,536 B), **64 at int8** (49,152 B, per 0081). Added: trap 7d (`iron.jit`'s cache key never reads a generator's module globals - sixth stale-binary fail-open, first inside IRON), trap 8 (IRON's `arg_types` shapes only the MLIR call site, never the linked object; and a design's host-facing dtype must independently track its pipeline's), a 3b extension for the shim DMA's missing compression/padding, and a 2b addendum for the T26 rounding hypothesis **explicitly marked unconfirmed**. CURRENT_STATUS: "Four bugs that failed open" -> **Five**, and section 9's next-steps list, still naming a tokenizer and MTEB as future work, replaced. Performance tables untouched | research | done |
| [0098](0098-t26-kernel-source/TASK.md) | **T26: the emulated matmul's rounding-mode swap is compiled away, and the anomaly may be trap 2b one level deeper.** `mm.cc:101/228` wraps its emulated-bf16 k-loop in `swap_rounding(conv_even)`/`set_rounding(saved)`, but the compiled object has **zero** `crrnd` writes while plainly containing the emulated path's `vconv.bfp16ebs8.fp32`. Cause: `mmul_bf16_bf16.hpp` calls the **ambient** intrinsic `::to_v64bfp16ebs8` (`aie2p_srs.h:1308`) instead of `to_v64bfp16ebs8_conf` (`:1331`), so nothing connects the save/restore pair to the quantisation between them and it is eliminated. Meanwhile `narrow_f32_bf16.o` carries three `mov crrnd, #0xc` and **never restores**. Net: an fp32-C core, which never calls `narrow()`, quantises A/B under the default **`floor`** forever — biased low, compounding over K — while a bf16-C core inherits `conv_even` from its own epilogue. Reproduces parity at stage 1, monotonic divergence with depth, **and 0053's split-mode shrinkage**, the one thing 0096's host model could not. Also re-confirmed 0053's "the wrappers differ only in X" by independent diff, and **ruled out** the extended-precision-accumulator hypothesis (`aie::accum<accfloat,N>` is plain 32-bit-per-lane). **Not confirmed on hardware**; two ablations proposed | research | done |
| [0093](0093-t11-t12-t13-research/TASK.md) | **Three cheap threads settled by reading, no hardware** — **T11 RETIRED**: partition width IS a driver-ioctl parameter (`__u32 num_tiles` in `amdxdna_drm_create_hwctx`) but unreachable from the XRT layer we call, whose `cfg_param_type` carries QoS keys only; both our runtime and IRON use the no-QoS constructor. **T12 ANSWERED**: 2602.06063's 5.9 -> 13.7 TOPS is not "megatile size alone" — 128x512x512 -> 256x256x512 is a **reshape at constant volume** worth 2.0x, and only the 4x volume step adds 1.14x; it is bf16 on a different XDNA2 SKU, and the mechanism already exists here as `b_reuse="mega"` + `tile_n` (~1.03-1.05x predicted on `ffn_up` at `tile_n=96`). **T13 stays OPEN, re-scoped**: 0058 reproduced the ~10% pretiled spread on the current toolchain with row-major at 0.0% in the same session (so not contention, not fixed by the 1.4 upgrade), while 0085 measures <0.6% end to end — different geometries, so neither settles the other | research | done |
